# Checkpoint 30 — Multi-Connector Entity Graph + Cross-System Correlation

Implemented canonical entities, graph edges, correlation snapshots, graph APIs, UI page, and graph-aware arbitration/simulation context.

## Replay Safety
- Correlation snapshots persist deterministic hash + engine version.
- Edge provenance includes source references and timestamps.

## Graph-Aware Intelligence
- Arbitration increases governance risk for privileged graph paths.
- Arbitration lowers confidence for orphan-linked entities.
- Simulation blast radius includes graph dependency chain factors.
