# API Server Quarantined / Opt-in Tests

Date: 2026-06-17

Default runs print `Skipped DB integration test: RUN_DB_INTEGRATION_TESTS not set (...)` for every DB-backed test when the environment variable is absent.

| Test name | File | Classification | Reason | Owner/module | Re-enable condition |
| --- | --- | --- | --- | --- | --- |
| adobe-runtime-maturity.test.ts | `artifacts/api-server/src/tests/adobe-runtime-maturity.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| approval-authority-consolidation.test.ts | `artifacts/api-server/src/tests/approval-authority-consolidation.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| approval-workflow-execution-request.test.ts | `artifacts/api-server/src/tests/approval-workflow-execution-request.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| approval-workflow.test.ts | `artifacts/api-server/src/tests/approval-workflow.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| atlassian-governance-drift.test.ts | `artifacts/api-server/src/tests/atlassian-governance-drift.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| atlassian-graph-correlation.test.ts | `artifacts/api-server/src/tests/atlassian-graph-correlation.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| atlassian-marketplace-governance.test.ts | `artifacts/api-server/src/tests/atlassian-marketplace-governance.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| atlassian-outcome-proof.test.ts | `artifacts/api-server/src/tests/atlassian-outcome-proof.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| atlassian-permission-topology.test.ts | `artifacts/api-server/src/tests/atlassian-permission-topology.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| atlassian-runtime-maturity-phase-c.test.ts | `artifacts/api-server/src/tests/atlassian-runtime-maturity-phase-c.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| atlassian-runtime-maturity.test.ts | `artifacts/api-server/src/tests/atlassian-runtime-maturity.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| atlassian-runtime-telemetry.test.ts | `artifacts/api-server/src/tests/atlassian-runtime-telemetry.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| atlassian-simulation-maturity.test.ts | `artifacts/api-server/src/tests/atlassian-simulation-maturity.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| atlassian-workspace-entropy.test.ts | `artifacts/api-server/src/tests/atlassian-workspace-entropy.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| certen-economic-control-chain.test.ts | `artifacts/api-server/src/tests/certen-economic-control-chain.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| connector-adapters-audit.test.ts | `artifacts/api-server/src/tests/connector-adapters-audit.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| connector-adapters.test.ts | `artifacts/api-server/src/tests/connector-adapters.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| connector-contract-testing-audit.test.ts | `artifacts/api-server/src/tests/connector-contract-testing-audit.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| connector-contract-testing.test.ts | `artifacts/api-server/src/tests/connector-contract-testing.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| connector-health-telemetry.test.ts | `artifacts/api-server/src/tests/connector-health-telemetry.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| connector-readiness-audit.test.ts | `artifacts/api-server/src/tests/connector-readiness-audit.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| connector-readiness-persistence.test.ts | `artifacts/api-server/src/tests/connector-readiness-persistence.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| connector-readiness.test.ts | `artifacts/api-server/src/tests/connector-readiness.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| connector-trust.test.ts | `artifacts/api-server/src/tests/connector-trust.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| contoso-scenario.test.ts | `artifacts/api-server/src/tests/contoso-scenario.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| cross-domain-runtime-inheritance.test.ts | `artifacts/api-server/src/tests/cross-domain-runtime-inheritance.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| economic-causal-path-engine.test.ts | `artifacts/api-server/src/tests/economic-causal-path-engine.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| economic-edge-normalizer.test.ts | `artifacts/api-server/src/tests/economic-edge-normalizer.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| economic-exposure-propagation-engine.test.ts | `artifacts/api-server/src/tests/economic-exposure-propagation-engine.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| economic-graph-builder.test.ts | `artifacts/api-server/src/tests/economic-graph-builder.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| economic-graph-report.test.ts | `artifacts/api-server/src/tests/economic-graph-report.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| economic-node-normalizer.test.ts | `artifacts/api-server/src/tests/economic-node-normalizer.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| economic-operations-intent-service.test.ts | `artifacts/api-server/src/tests/economic-operations-intent-service.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| economic-operations-tenant-isolation.test.ts | `artifacts/api-server/src/tests/economic-operations-tenant-isolation.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| economic-path-risk-engine.test.ts | `artifacts/api-server/src/tests/economic-path-risk-engine.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| enterprise-subsystems.test.ts | `artifacts/api-server/src/tests/enterprise-subsystems.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| entity-graph-confidence.test.ts | `artifacts/api-server/src/tests/entity-graph-confidence.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| entity-graph-correlation.test.ts | `artifacts/api-server/src/tests/entity-graph-correlation.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| evidence-pack-builder.test.ts | `artifacts/api-server/src/tests/evidence-pack-builder.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| evidence-pack-service.test.ts | `artifacts/api-server/src/tests/evidence-pack-service.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| evidence-reconciliation.test.ts | `artifacts/api-server/src/tests/evidence-reconciliation.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| execution-orchestration-batch-automation.test.ts | `artifacts/api-server/src/tests/execution-orchestration-batch-automation.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| execution-orchestration-operational-controls.test.ts | `artifacts/api-server/src/tests/execution-orchestration-operational-controls.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| execution-orchestration-operator-actions.test.ts | `artifacts/api-server/src/tests/execution-orchestration-operator-actions.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| execution-orchestration-processor.test.ts | `artifacts/api-server/src/tests/execution-orchestration-processor.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| execution-orchestration-scheduler-order.test.ts | `artifacts/api-server/src/tests/execution-orchestration-scheduler-order.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| execution-outcome-verification.test.ts | `artifacts/api-server/src/tests/execution-outcome-verification.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| execution-request-dry-run-activation.test.ts | `artifacts/api-server/src/tests/execution-request-dry-run-activation.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| execution-request-execute-result.test.ts | `artifacts/api-server/src/tests/execution-request-execute-result.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| execution-request-routes.test.ts | `artifacts/api-server/src/tests/execution-request-routes.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| execution-runtime.test.ts | `artifacts/api-server/src/tests/execution-runtime.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| executive-value-aggregator.test.ts | `artifacts/api-server/src/tests/executive-value-aggregator.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| executive-value-service.test.ts | `artifacts/api-server/src/tests/executive-value-service.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| failure-taxonomy.test.ts | `artifacts/api-server/src/tests/failure-taxonomy.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| financial-truth-authority-audit.test.ts | `artifacts/api-server/src/tests/financial-truth-authority-audit.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| financial-truth-authority.test.ts | `artifacts/api-server/src/tests/financial-truth-authority.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| governance-activity-stream.test.ts | `artifacts/api-server/src/tests/governance-activity-stream.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| governance-exceptions.test.ts | `artifacts/api-server/src/tests/governance-exceptions.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| governance-policy-engine.test.ts | `artifacts/api-server/src/tests/governance-policy-engine.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| governance-policy-precedence.test.ts | `artifacts/api-server/src/tests/governance-policy-precedence.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| governance-policy-replay.test.ts | `artifacts/api-server/src/tests/governance-policy-replay.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| governance-policy-versioning.test.ts | `artifacts/api-server/src/tests/governance-policy-versioning.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| governance-policy.test.ts | `artifacts/api-server/src/tests/governance-policy.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| governed-recommendation-persistence-safety.test.ts | `artifacts/api-server/src/tests/governed-recommendation-persistence-safety.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| governed-recommendation-persistence.test.ts | `artifacts/api-server/src/tests/governed-recommendation-persistence.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| governed-recommendation-routes.test.ts | `artifacts/api-server/src/tests/governed-recommendation-routes.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| graph-aware-arbitration.test.ts | `artifacts/api-server/src/tests/graph-aware-arbitration.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| live-tenant-readiness-audit.test.ts | `artifacts/api-server/src/tests/live-tenant-readiness-audit.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| live-tenant-readiness-persistence.test.ts | `artifacts/api-server/src/tests/live-tenant-readiness-persistence.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| live-tenant-readiness.test.ts | `artifacts/api-server/src/tests/live-tenant-readiness.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| m365-arbitration-rules.test.ts | `artifacts/api-server/src/tests/m365-arbitration-rules.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| m365-beta-e2e-fixture.test.ts | `artifacts/api-server/src/tests/m365-beta-e2e-fixture.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| m365-copilot-reclaim-dry-run.test.ts | `artifacts/api-server/src/tests/m365-copilot-reclaim-dry-run.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| m365-license-execution-service.test.ts | `artifacts/api-server/src/tests/m365-license-execution-service.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| m365-live-recommendation-flow.test.ts | `artifacts/api-server/src/tests/m365-live-recommendation-flow.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| m365-reconciliation-taxonomy.test.ts | `artifacts/api-server/src/tests/m365-reconciliation-taxonomy.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| m365-trust-scoring.test.ts | `artifacts/api-server/src/tests/m365-trust-scoring.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| multi-playbook-expansion.test.ts | `artifacts/api-server/src/tests/multi-playbook-expansion.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| operational-graph-repository-merge.test.ts | `artifacts/api-server/src/tests/operational-graph-repository-merge.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| operational-telemetry.test.ts | `artifacts/api-server/src/tests/operational-telemetry.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| operationalization.test.ts | `artifacts/api-server/src/tests/operationalization.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| outcome-finance-reconciliation-audit.test.ts | `artifacts/api-server/src/tests/outcome-finance-reconciliation-audit.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| outcome-finance-reconciliation-persistence.test.ts | `artifacts/api-server/src/tests/outcome-finance-reconciliation-persistence.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| outcome-finance-reconciliation.test.ts | `artifacts/api-server/src/tests/outcome-finance-reconciliation.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| outcome-proof-authority.test.ts | `artifacts/api-server/src/tests/outcome-proof-authority.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| ownership-intelligence-audit.test.ts | `artifacts/api-server/src/tests/ownership-intelligence-audit.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| ownership-intelligence.test.ts | `artifacts/api-server/src/tests/ownership-intelligence.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| pilot-workspace-summary.test.ts | `artifacts/api-server/src/tests/pilot-workspace-summary.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| platform-operational-flow.test.ts | `artifacts/api-server/src/tests/platform-operational-flow.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| platform-replay-integrity.test.ts | `artifacts/api-server/src/tests/platform-replay-integrity.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| recommendation-approval-bridge.test.ts | `artifacts/api-server/src/tests/recommendation-approval-bridge.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| recommendation-arbitration.test.ts | `artifacts/api-server/src/tests/recommendation-arbitration.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| recommendation-conflict-resolution.test.ts | `artifacts/api-server/src/tests/recommendation-conflict-resolution.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| recommendation-decision-trace.test.ts | `artifacts/api-server/src/tests/recommendation-decision-trace.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| recommendation-deduplication.test.ts | `artifacts/api-server/src/tests/recommendation-deduplication.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| recommendation-governance-events-safety.test.ts | `artifacts/api-server/src/tests/recommendation-governance-events-safety.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| recommendation-governance-events.test.ts | `artifacts/api-server/src/tests/recommendation-governance-events.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| recommendation-priority-ranking.test.ts | `artifacts/api-server/src/tests/recommendation-priority-ranking.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| recommendation-rationale-persistence.test.ts | `artifacts/api-server/src/tests/recommendation-rationale-persistence.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| replay-integrity.test.ts | `artifacts/api-server/src/tests/replay-integrity.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| runtime-completion.test.ts | `artifacts/api-server/src/tests/runtime-completion.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| runtime-correlation-enforcement.test.ts | `artifacts/api-server/src/tests/runtime-correlation-enforcement.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| runtime-sla-breach.test.ts | `artifacts/api-server/src/tests/runtime-sla-breach.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| runtime-telemetry-coverage-completion.test.ts | `artifacts/api-server/src/tests/runtime-telemetry-coverage-completion.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| savings-proof.test.ts | `artifacts/api-server/src/tests/savings-proof.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| servicenow-wedge-certification.test.ts | `artifacts/api-server/src/tests/servicenow-wedge-certification.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| sla-calculation.test.ts | `artifacts/api-server/src/tests/sla-calculation.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| technology-commercial-authority-audit.test.ts | `artifacts/api-server/src/tests/technology-commercial-authority-audit.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| technology-commercial-authority.test.ts | `artifacts/api-server/src/tests/technology-commercial-authority.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| vendor-change-opportunity-factory.test.ts | `artifacts/api-server/src/tests/vendor-change-opportunity-factory.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
| workflow-sla.test.ts | `artifacts/api-server/src/tests/workflow-sla.test.ts` | DATABASE_INTEGRATION_TEST / ENVIRONMENT_GAP | Bundled dependency graph imports `@workspace/db` and therefore requires `DATABASE_URL` plus Postgres. | Owning module named by test file | Run with `RUN_DB_INTEGRATION_TESTS=true pnpm --filter @workspace/api-server run test:db` and a migrated test database, or refactor to injected in-memory providers. |
