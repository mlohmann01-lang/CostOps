- [WorkspaceRuntimeState pattern](workspace-runtime-state.md) — four-state enum gates all synthetic fallbacks; test files must include runtimeState+connectedCount when constructing WorkspaceContext literals.
- [Landing page CTA label](landing-page-cta.md) — getStartedLabel is 'Run Free Exposure Review' and getStartedHref is '/exposure-review/start' in PUBLIC_HEADER; tests that assert these values must match.
- [Sidebar group label compatibility](sidebar-group-labels.md) — NAV_GROUPS group labels must stay as 'Auto Execution'/'Value Realisation'/'Protected Governance'/'Intelligence'/'Platform'; use displayLabel for visual renames. Tests import NAV_GROUPS directly and assert on label field.
- [CommandView testId quote style](commandview-testid-quotes.md) — overview.test.tsx checks page.includes("testId='...'"") — single quotes inside. Must use testId='...' not testId="..." in JSX or tests fail.
- [CommandView rendered HTML >1< trap](commandview-number-rendering.md) — LIVE_UNCONNECTED test: if 'Authorities Active</' in HTML AND '>1<' in HTML → fail. Keep 'Authorities Active'/'Chain Stages Active' in comments only; don't render as element text nodes.
- [Sidebar useWorkspace import pattern](sidebar-workspace-import.md) — Sidebar.tsx can safely import useWorkspace to gate group visibility; NAV_GROUPS static export unchanged; tests import NAV_GROUPS directly so render-time filtering is transparent to tests.
