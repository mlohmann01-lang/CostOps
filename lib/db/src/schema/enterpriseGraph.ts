import { pgTable, serial, text, timestamp, jsonb, real } from "drizzle-orm/pg-core";
export const enterpriseEntitiesTable = pgTable("enterprise_entities", { id: serial("id").primaryKey(), tenantId: text("tenant_id").notNull(), entityType: text("entity_type").notNull(), entityKey: text("entity_key").notNull(), displayName: text("display_name"), confidence: real("confidence").notNull().default(0), createdAt: timestamp("created_at",{withTimezone:true}).notNull().defaultNow() });
export const enterpriseRelationshipsTable = pgTable("enterprise_relationships", { id: serial("id").primaryKey(), tenantId: text("tenant_id").notNull(), fromEntityKey: text("from_entity_key").notNull(), toEntityKey: text("to_entity_key").notNull(), relationshipType: text("relationship_type").notNull(), confidence: real("confidence").notNull().default(0), evidence: jsonb("evidence").notNull().default({}), createdAt: timestamp("created_at",{withTimezone:true}).notNull().defaultNow() });
export const entityEvidenceTable = pgTable("entity_evidence", { id: serial("id").primaryKey(), tenantId: text("tenant_id").notNull(), entityKey: text("entity_key").notNull(), source: text("source").notNull(), evidence: jsonb("evidence").notNull().default({}), createdAt: timestamp("created_at",{withTimezone:true}).notNull().defaultNow() });
export const entityResolutionEventsTable = pgTable("entity_resolution_events", { id: serial("id").primaryKey(), tenantId: text("tenant_id").notNull(), entityKey: text("entity_key").notNull(), eventType: text("event_type").notNull(), message: text("message").notNull(), evidence: jsonb("evidence").notNull().default({}), createdAt: timestamp("created_at",{withTimezone:true}).notNull().defaultNow() });
