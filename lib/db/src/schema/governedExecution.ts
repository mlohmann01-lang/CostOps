import { pgTable, text, jsonb, timestamp, uniqueIndex, index } from 'drizzle-orm/pg-core';
export const governedExecutionRecordsTable = pgTable('governed_execution_records', { id: text('id').primaryKey(), tenantId: text('tenant_id').notNull(), collection: text('collection').notNull(), recordId: text('record_id').notNull(), payload: jsonb('payload').notNull().default({}), createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(), updatedAt: timestamp('updated_at', { withTimezone: true }).notNull().defaultNow().$onUpdate(() => new Date()) }, (table) => ({ tenantCollectionRecordIdx: uniqueIndex('governed_execution_tenant_collection_record_idx').on(table.tenantId, table.collection, table.recordId), tenantCollectionIdx: index('governed_execution_tenant_collection_idx').on(table.tenantId, table.collection) }));
