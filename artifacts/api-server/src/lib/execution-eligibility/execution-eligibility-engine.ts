import type {ExecutionEligibilityInput} from './execution-eligibility-types';import {evaluateGovernanceExecutionGate} from './execution-governance-gate';import {evaluateExecutionPolicyCompliance} from './execution-policy-check';export const evaluateExecutionEligibility=(input:ExecutionEligibilityInput)=>{const gate=evaluateGovernanceExecutionGate(input);const policy=evaluateExecutionPolicyCompliance(input);if(!policy.compliant&&gate==='GOVERNED_EXECUTION_ELIGIBLE'){return'APPROVAL_REQUIRED';}return gate;};