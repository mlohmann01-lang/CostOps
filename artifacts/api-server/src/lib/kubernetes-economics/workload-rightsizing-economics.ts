import type { KubernetesNormalizedSignal, WorkloadRightsizingEconomicsResult } from './kubernetes-economic-types';
export function evaluateWorkloadRightsizingEconomics(s:KubernetesNormalizedSignal):WorkloadRightsizingEconomicsResult { const cpuGap=Math.max(0,s.requestedCpu-s.usedCpu); const memGap=Math.max(0,s.requestedMemory-s.usedMemory); const waste=s.monthlyCost*Math.min(0.8,(cpuGap+memGap)*0.05); const underRisk=((s.usedCpu>s.requestedCpu*0.95)||(s.usedMemory>s.requestedMemory*0.95))?0.7:0.2; const prod=s.environmentType==='PRODUCTION'?0.65:1; const score=Math.max(0,1-(cpuGap+memGap)*0.1*prod); return { rightsizingScore:score, overRequestWasteMonthly:waste, underRequestRisk:underRisk, recommendedReviewMode:(underRisk>0.6||s.environmentType==='PRODUCTION')?'APPROVAL_REQUIRED':'RECOMMEND_ONLY', confidence:0.75, reasons:[cpuGap>0.2?'CPU over-requesting observed':'CPU request near usage', memGap>0.2?'Memory over-requesting observed':'Memory request near usage', underRisk>0.6?'Under-requesting risk surfaced':'Under-requesting risk low'] }; }
