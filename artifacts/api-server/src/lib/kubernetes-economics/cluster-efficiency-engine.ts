import type { ClusterEfficiencyResult, KubernetesNormalizedSignal } from './kubernetes-economic-types';
export function evaluateClusterEfficiency(signals:KubernetesNormalizedSignal[]):ClusterEfficiencyResult { const waste=signals.reduce((s,x)=>s+Math.max(0,(x.requestedCpu-x.usedCpu)*x.monthlyCost*0.02)+Math.max(0,(x.requestedMemory-x.usedMemory)*x.monthlyCost*0.01),0); const total=signals.reduce((s,x)=>s+x.monthlyCost,0); const eff=Math.max(0,1-(waste/Math.max(1,total))); return { clusterEfficiencyScore:eff, estimatedMonthlyWaste:waste, estimatedAnnualWaste:waste*12, topWasteDrivers:['CPU overprovisioning','Memory overprovisioning','Autoscaling mismatch'], clusterRisk:1-eff, confidence:0.75 }; }
