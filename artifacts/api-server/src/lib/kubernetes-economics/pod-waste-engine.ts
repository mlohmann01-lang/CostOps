import type { KubernetesNormalizedSignal, PodWasteResult } from './kubernetes-economic-types';
export function evaluatePodWaste(s:KubernetesNormalizedSignal):PodWasteResult { const drivers=[] as string[]; const n=(s.crashLoopPods??0)+(s.completedJobsNotCleaned??0)+(s.orphanedPods??0)+(s.idlePods??0)+(s.lowRequestPods??0)+(s.staleReplicas??0); if((s.crashLoopPods??0)>0) drivers.push('Crashlooping pods'); if((s.completedJobsNotCleaned??0)>0) drivers.push('Completed jobs not cleaned'); if((s.orphanedPods??0)>0) drivers.push('Orphaned pods'); return { podWasteScore:Math.min(1,n/20), estimatedMonthlyWaste:s.monthlyCost*Math.min(0.6,n*0.03), wasteDrivers:drivers.length?drivers:['No significant pod waste drivers'], cleanupReviewRecommended:n>2, confidence:0.7 }; }
