import type { EphemeralWorkloadEconomicsResult, KubernetesNormalizedSignal } from './kubernetes-economic-types';
export function evaluateEphemeralWorkloadEconomics(signals:KubernetesNormalizedSignal[]):EphemeralWorkloadEconomicsResult { const jobs=signals.filter(s=>s.workloadType==='JOB'||s.workloadType==='CRONJOB'); const failed=jobs.filter(j=>(j.crashLoopPods??0)>0||(j.completedJobsNotCleaned??0)>0).length; const score=Math.min(1,(jobs.length?failed/jobs.length:0)*0.6+(signals.some(s=>s.environmentType==='SANDBOX')?0.25:0)); const waste=signals.reduce((s,x)=>s+x.monthlyCost,0)*score*0.2; return { ephemeralWasteScore:score, estimatedMonthlyWaste:waste, ttlPolicyRecommended:score>0.3, cleanupReviewRecommended:failed>0, confidence:0.7 }; }
