import type {ExecutiveProofPack,ExecutiveProofPackEvidenceBinding} from './executive-proof-pack-types';
export function isEvidenceExportSafe(e:any){return e?.redactionStatus!=='PENDING'&&e?.redactionStatus!=='FAILED'&&e?.classification!=='SECRET'&&e?.integrityStatus!=='FAIL'}
export function buildEvidenceBindings(pack:ExecutiveProofPack,evidence:any[]=[]):ExecutiveProofPackEvidenceBinding[]{const now=new Date().toISOString(); const refs=evidence.length?evidence:[...(pack.metrics.evidenceRefs as any[]??[]),...pack.sectionIds.map((s,i)=>({evidenceRef:`${pack.id}-evidence-${i+1}`,integrityStatus:'PASS',redactionStatus:'NOT_REQUIRED',classification:'INTERNAL'}))]; return refs.map((e:any,i:number)=>({id:`${pack.id}-binding-${i+1}`,tenantId:pack.tenantId,packId:pack.id,sectionId:pack.sectionIds[i%Math.max(1,pack.sectionIds.length)],evidenceRef:e.evidenceRef??e.id??String(e),bindingType:(['PROVES_VALUE','PROVES_AUDIT','PROVES_RISK','PROVES_OWNERSHIP','SUPPORTS_METRIC'] as const)[i%5],targetMetric:i===0?'financeVerifiedSavings':undefined,trustLevel:e.trustLevel??'HIGH',integrityStatus:e.integrityStatus??'PASS',redactionStatus:e.redactionStatus??'NOT_REQUIRED',exportSafe:isEvidenceExportSafe(e),createdAt:now,updatedAt:now}))}
