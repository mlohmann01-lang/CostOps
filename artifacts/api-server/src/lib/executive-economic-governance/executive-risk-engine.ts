import type { ExecutiveEconomicRisk } from './executive-governance-types';
export function computeExecutiveEconomicRisk(input:{riskSignals:Record<ExecutiveEconomicRisk['riskType'],number>;trend:'IMPROVING'|'STABLE'|'WORSENING';forecastWindow:ExecutiveEconomicRisk['forecastWindow'];}):ExecutiveEconomicRisk[]{ return (Object.keys(input.riskSignals) as ExecutiveEconomicRisk['riskType'][]).map((riskType)=>({ riskType, severity:Math.max(0,Math.min(1,input.riskSignals[riskType])), trajectory:input.trend, confidence:0.7, mitigationGuidance:['Increase governance controls','Strengthen owner accountability','Tighten approval rigor'], forecastWindow:input.forecastWindow })); }
