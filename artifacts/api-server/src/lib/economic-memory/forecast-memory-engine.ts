import type { CalibrationMemorySummary, ForecastMemoryAdjustment } from './economic-memory-types';
export function deriveForecastMemoryAdjustment(summary:CalibrationMemorySummary):ForecastMemoryAdjustment { const reasons:string[]=[]; let confidenceAdjustment=0; let uncertaintyPenalty=0; if(summary.consistencyClass==='CONSISTENT_ACCURACY'&&summary.averageErrorRate<0.15){ confidenceAdjustment=0.08; reasons.push('Historical calibration is consistently accurate.'); } if(summary.biasClass!=='BALANCED_BIAS'){ confidenceAdjustment-=0.05; uncertaintyPenalty+=0.08; reasons.push('Systemic forecast bias detected in memory.'); } if(summary.consistencyClass==='VOLATILE_ACCURACY'){ confidenceAdjustment-=0.1; uncertaintyPenalty+=0.15; reasons.push('Historical error volatility is elevated.'); } if(summary.consistencyClass==='LOW_EVIDENCE_MEMORY'){ confidenceAdjustment-=0.12; uncertaintyPenalty+=0.2; reasons.push('Low evidence memory weakens reliability.'); } const recommendedForecastTreatment=summary.consistencyClass==='LOW_EVIDENCE_MEMORY'||summary.averageErrorRate>0.35?'MARK_SPECULATIVE':confidenceAdjustment>0.04?'BOOST_CONFIDENCE':confidenceAdjustment<-0.03?'REDUCE_CONFIDENCE':'MAINTAIN_CONFIDENCE'; return { confidenceAdjustment, forecastBias:summary.biasClass==='OVER_BIAS'?'OVER_ESTIMATION':summary.biasClass==='UNDER_BIAS'?'UNDER_ESTIMATION':'BALANCED', uncertaintyPenalty, recommendedForecastTreatment, memoryReasons:reasons.length?reasons:['Memory profile is stable.'] }; }
