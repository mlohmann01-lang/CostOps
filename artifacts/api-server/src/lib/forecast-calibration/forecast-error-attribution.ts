export const evaluateForecastErrorAttribution=(i:{workload:number;pricing:number;approvalDelay:number;recurrence:number;missingEvidence:number})=>{const vals=[i.workload,i.pricing,i.approvalDelay,i.recurrence,i.missingEvidence];const keys=["workload","pricing","approvalDelay","recurrence","missingEvidence"];const idx=vals.indexOf(Math.max(...vals));return {primary:keys[idx],deterministicForecast:true};};
