import type { ProductionConnectorNormalisationResult, RawProductionRecord } from "./production-connector-types";
export function normalisedPass(connectorKey: string, record: RawProductionRecord, outputContract: string, normalised: Record<string, unknown>, evidenceRefs: string[] = []): ProductionConnectorNormalisationResult { return { connectorKey, outputContract, status: "PASS", rawRecordId: record.id, normalised, errors: [], evidenceRefs }; }
export function normalisedWarn(connectorKey: string, record: RawProductionRecord, outputContract: string, normalised: Record<string, unknown>, code: string, message: string): ProductionConnectorNormalisationResult { return { connectorKey, outputContract, status: "WARN", rawRecordId: record.id, normalised, errors: [{ code, message }], evidenceRefs: [] }; }
export function normalisedFail(connectorKey: string, record: RawProductionRecord, code: string, message: string): ProductionConnectorNormalisationResult { return { connectorKey, outputContract: "UNKNOWN", status: "FAIL", rawRecordId: record.id, errors: [{ code, message }], evidenceRefs: [] }; }
