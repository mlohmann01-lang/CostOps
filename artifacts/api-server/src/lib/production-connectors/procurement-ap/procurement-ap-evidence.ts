import crypto from'node:crypto';import{EvidenceRegistryRepository}from'../../evidence-registry/evidence-registry-repository';export async function writeProcurementApEvidence(repo:EvidenceRegistryRepository,tenantId:string,targetId:string,title:string,eventType:string,payload:unknown){const evidenceRef=`procurement-ap-${eventType.toLowerCase()}-${targetId}`,hash=crypto.createHash('sha256').update(JSON.stringify(payload)).digest('hex'),now=new Date().toISOString();await repo.upsertEvidenceRecord({id:evidenceRef,tenantId,evidenceRef,title,targetType:'CONNECTOR' as any,targetId,status:'COLLECTED' as any,evidenceType:'OTHER' as any,classification:'INTERNAL' as any,source:'PROCUREMENT_AP',metadata:{eventType,hash},createdAt:now,updatedAt:now}as any);await repo.upsertArtifact({id:`artifact-${evidenceRef}`,tenantId,evidenceRef,artifactType:'JSON' as any,metadata:{eventType},contentHash:hash,hashAlgorithm:'SHA256' as any,sizeBytes:JSON.stringify(payload).length,createdAt:now,updatedAt:now}as any);return evidenceRef}
