import type {ExecutionPlanInput} from './execution-plan-types';import {buildExecutionPlan} from './execution-plan-builder';import {modelApprovalChain} from './approval-chain-model';export const validateExecutionPlan=(i:ExecutionPlanInput)=>{const plan=buildExecutionPlan(i);const chain=modelApprovalChain(i);const needsChain=plan.state==='PLAN_REQUIRES_APPROVAL';return{valid:!needsChain||Boolean(chain.chainId),plan};};