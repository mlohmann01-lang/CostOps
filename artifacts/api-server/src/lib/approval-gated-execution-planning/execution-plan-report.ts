import type {ExecutionPlanInput} from './execution-plan-types';import {buildExecutionPlan} from './execution-plan-builder';import {evaluateApprovalRequirement} from './approval-requirement-engine';import {modelApprovalChain} from './approval-chain-model';import {evaluateChangeWindow} from './change-window-model';import {validateExecutionPlan} from './execution-plan-validator';export const computeExecutionPlanReport=(i:ExecutionPlanInput)=>({plan:buildExecutionPlan(i),requiresApproval:evaluateApprovalRequirement(i),approval:modelApprovalChain(i),window:evaluateChangeWindow(i),validation:validateExecutionPlan(i)});