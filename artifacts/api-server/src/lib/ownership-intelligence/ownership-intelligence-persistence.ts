import type { OwnershipCollection, PersistenceStore } from './ownership-intelligence-types';
const matches=(v:Record<string,unknown>,f:Record<string,unknown>)=>Object.entries(f).every(([k,val])=>val===undefined||val===''||v[k]===val);
export class MemoryPersistenceStore<T extends {id:string;tenantId:string}> implements PersistenceStore<T>{private rows=new Map<string,T>();constructor(readonly collection:OwnershipCollection){} private key(t:string,id:string){return `${t}:${id}`} async upsert(v:T){this.rows.set(this.key(v.tenantId,v.id),v);return v} async get(t:string,id:string){return this.rows.get(this.key(t,id))} async list(t:string,f:Record<string,unknown>={}){return [...this.rows.values()].filter(v=>v.tenantId===t&&matches(v as any,f))} async deleteTenant(t:string){for(const v of this.rows.values())if(v.tenantId===t)this.rows.delete(this.key(t,v.id))} async size(){return this.rows.size}}
export class DatabasePersistenceStore<T extends {id:string;tenantId:string}> implements PersistenceStore<T>{constructor(readonly collection:OwnershipCollection){} private pk(v:{tenantId:string;id:string}){return `${v.tenantId}:${this.collection}:${v.id}`} private async db(){return import('@workspace/db')} async upsert(v:T){const {db,ownershipIntelligenceRecordsTable}=await this.db(); await db.insert(ownershipIntelligenceRecordsTable).values({id:this.pk(v),tenantId:v.tenantId,collection:this.collection,recordId:v.id,payload:v}).onConflictDoUpdate({target:ownershipIntelligenceRecordsTable.id,set:{payload:v,updatedAt:new Date()}}); return v} async get(t:string,id:string){const {db,ownershipIntelligenceRecordsTable}=await this.db(); const {and,eq}=await import('drizzle-orm'); const rows=await db.select().from(ownershipIntelligenceRecordsTable).where(and(eq(ownershipIntelligenceRecordsTable.tenantId,t),eq(ownershipIntelligenceRecordsTable.collection,this.collection),eq(ownershipIntelligenceRecordsTable.recordId,id))).limit(1); return rows[0]?.payload as T|undefined} async list(t:string,f:Record<string,unknown>={}){const {db,ownershipIntelligenceRecordsTable}=await this.db(); const {and,eq}=await import('drizzle-orm'); const rows=await db.select().from(ownershipIntelligenceRecordsTable).where(and(eq(ownershipIntelligenceRecordsTable.tenantId,t),eq(ownershipIntelligenceRecordsTable.collection,this.collection))); return rows.map(r=>r.payload as T).filter(v=>matches(v as any,f))} async deleteTenant(t:string){const {db,ownershipIntelligenceRecordsTable}=await this.db(); const {and,eq}=await import('drizzle-orm'); await db.delete(ownershipIntelligenceRecordsTable).where(and(eq(ownershipIntelligenceRecordsTable.tenantId,t),eq(ownershipIntelligenceRecordsTable.collection,this.collection)))} async size(){const {db,ownershipIntelligenceRecordsTable}=await this.db(); const {eq}=await import('drizzle-orm'); return (await db.select().from(ownershipIntelligenceRecordsTable).where(eq(ownershipIntelligenceRecordsTable.collection,this.collection))).length}}
export function createPersistenceStore<T extends {id:string;tenantId:string}>(collection:OwnershipCollection):PersistenceStore<T>{return process.env.DATABASE_URL?new DatabasePersistenceStore<T>(collection):new MemoryPersistenceStore<T>(collection)}
