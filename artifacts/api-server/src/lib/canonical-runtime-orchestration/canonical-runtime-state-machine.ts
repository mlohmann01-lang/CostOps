import type { RuntimePhase, RuntimeState } from "./canonical-runtime-types"; export const evaluateCanonicalRuntimeState=(i:{phase:RuntimePhase;blocked:boolean;waitingApproval:boolean;executing:boolean;verified:boolean;failed:boolean;completed:boolean}):RuntimeState=>i.failed?"FAILED":i.blocked?"BLOCKED":i.waitingApproval?"WAITING_FOR_APPROVAL":i.executing?"EXECUTING":i.verified?"VERIFIED":i.completed?"COMPLETED":"IN_PROGRESS";
