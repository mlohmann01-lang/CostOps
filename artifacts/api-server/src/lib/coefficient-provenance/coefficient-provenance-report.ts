import { evaluateEvidenceGrade } from "./coefficient-evidence-grade"; import { evaluateValidityWindow } from "./coefficient-validity-window"; export const computeCoefficientProvenanceReport=(i:any)=>({items:i.items.map((x:any)=>({...x,evidenceScore:evaluateEvidenceGrade(x.evidenceGrade),stale:evaluateValidityWindow(x,i.now).stale,adjustedConfidence:evaluateValidityWindow(x,i.now).stale?x.confidence*0.8:x.confidence}))});
