import { fetchServiceNowSample } from './servicenow-client';
export async function checkServiceNowReadiness(){ const missing=['SERVICENOW_INSTANCE_URL','SERVICENOW_CLIENT_ID','SERVICENOW_CLIENT_SECRET'].filter((k)=>!process.env[k]); if(missing.length>0 && (process.env.SERVICENOW_MODE??'MOCK_CONNECTOR')!=='MOCK_CONNECTOR') return {status:'BLOCKED',connectorHealth:'FAILED',warnings:[],errors:[`Missing env: ${missing.join(',')}`]}; try{ const sample=await fetchServiceNowSample(); return {status:'READY',connectorHealth:'HEALTHY',warnings:[],errors:[],requestId:sample.requestId,counts:{assets:sample.assets.length,contracts:sample.contracts.length}};} catch(e){ return {status:'BLOCKED',connectorHealth:'FAILED',warnings:[],errors:[e instanceof Error?e.message:'AUTH_FAILED']}; }}
