import { governedActionService, type GovernedAction } from "../../actions/governed-actions";
import { governedExecutionService } from "../../execution/governed-execution";
import { connectorSupportsCapability } from "../../execution/execution-connectors";
import { economicOutcomeAttributionService } from "../../economic-outcomes/economic-outcome-attribution";
import { outcomeProtectionService } from "../../outcome-protection/outcome-protection";
import { listAzureArtifacts } from "./azure-artifacts";

export type AzureExecutionClassification = "REAL_PROVIDER_EXECUTION" | "CONTROLLED_EXECUTION" | "SIMULATED_ONLY" | "NOT_IMPLEMENTED";
export type AzureCertificationStatus = "COMPLETE" | "MISSING";
export type AzureRollbackStatus = "COMPLETE" | "MISSING" | "PARTIAL" | "NOT_APPLICABLE";
export type AzurePlaybookCertification = { playbookId: string; name: string; discovery: AzureCertificationStatus; trust: AzureCertificationStatus; approval: AzureCertificationStatus; execution: AzureExecutionClassification; rollback: AzureRollbackStatus; verification: AzureCertificationStatus; outcome: AzureCertificationStatus; protection: AzureCertificationStatus; drift: AzureCertificationStatus; executiveProof: AzureCertificationStatus; certified: boolean; blockers: string[] };
const playbooks = [
  { playbookId: "azure-vm-rightsizing", name: "Azure VM Rightsizing", terms: ["azure", "vm", "right"], executionTypes: ["AZURE_RIGHTSIZE_VM"], capabilities: ["READ_VM", "MODIFY_VM", "VERIFY_RESOURCE_STATE", "ROLLBACK_RESOURCE"] },
  { playbookId: "azure-idle-resource-shutdown", name: "Idle Resource Shutdown", terms: ["idle", "shutdown"], executionTypes: ["AZURE_STOP_VM", "AZURE_STOP_SQL"], capabilities: ["READ_VM", "STOP_VM", "START_VM", "READ_SQL", "STOP_SQL", "START_SQL", "VERIFY_RESOURCE_STATE", "ROLLBACK_RESOURCE"] },
  { playbookId: "azure-reserved-instance-coverage", name: "Reserved Instance Coverage", terms: ["reserved", "instance"], executionTypes: ["AZURE_CREATE_RI_REVIEW"], capabilities: ["READ_COST_MANAGEMENT", "READ_CONSUMPTION", "VERIFY_RESOURCE_STATE"] },
  { playbookId: "azure-savings-plan-optimisation", name: "Savings Plan Optimisation", terms: ["savings", "plan"], executionTypes: ["AZURE_CREATE_SAVINGS_PLAN_REVIEW"], capabilities: ["READ_COST_MANAGEMENT", "READ_CONSUMPTION", "VERIFY_RESOURCE_STATE"] },
  { playbookId: "azure-orphaned-resource-cleanup", name: "Orphaned Resource Cleanup", terms: ["orphaned", "cleanup"], executionTypes: ["AZURE_DELETE_MANAGED_DISK", "AZURE_RELEASE_PUBLIC_IP"], capabilities: ["READ_MANAGED_DISK", "DELETE_MANAGED_DISK", "READ_PUBLIC_IP", "RELEASE_PUBLIC_IP", "VERIFY_RESOURCE_STATE", "ROLLBACK_RESOURCE"] },
] as const;
function complete(value: boolean): AzureCertificationStatus { return value ? "COMPLETE" : "MISSING"; }
function matches(action: GovernedAction, playbook: typeof playbooks[number]) { const h = [action.id, action.title, action.description, action.sourceId, ...action.recommendationIds, ...action.evidenceIds].join(" ").toLowerCase(); return h.includes(playbook.playbookId) || playbook.terms.every((term) => h.includes(term)); }
function blockers(row: Omit<AzurePlaybookCertification, "blockers" | "certified">, missingCapabilities: string[], partialAllowed: boolean) { const out: string[] = []; if (row.discovery !== "COMPLETE") out.push("Discovery evidence missing"); if (row.trust !== "COMPLETE") out.push("Trust Authority evidence missing"); if (row.approval !== "COMPLETE") out.push("Approval Authority evidence missing"); if (!["CONTROLLED_EXECUTION", "REAL_PROVIDER_EXECUTION"].includes(row.execution)) out.push(`Execution is ${row.execution}`); if (row.rollback === "MISSING" || (row.rollback === "PARTIAL" && !partialAllowed)) out.push("Rollback evidence missing or unjustified"); if (row.verification !== "COMPLETE") out.push("Verification evidence missing"); if (row.outcome !== "COMPLETE") out.push("Economic outcome missing"); if (row.protection !== "COMPLETE") out.push("Protected outcome missing"); if (row.drift !== "COMPLETE") out.push("Drift monitoring missing"); if (row.executiveProof !== "COMPLETE") out.push("Executive proof missing"); if (missingCapabilities.length) out.push(`Missing Azure capabilities: ${missingCapabilities.join(",")}`); return out; }
export async function getAzureWedgeCertification(tenantId: string) {
  const actions = await governedActionService.list(tenantId); const executions = governedExecutionService.listExecutions(tenantId); const connectors = governedExecutionService.listConnectors(tenantId).filter((connector) => connector.connectorType === "AZURE" && connector.status === "CONNECTED"); const artifacts = listAzureArtifacts(tenantId); const outcomes = economicOutcomeAttributionService.listEconomicOutcomes(tenantId, { assetType: "CLOUD" }); const protectedOutcomes = outcomeProtectionService.listProtectedOutcomes(tenantId, { assetType: "CLOUD" });
  const playbookStatus = playbooks.map<AzurePlaybookCertification>((playbook) => { const playbookActions = actions.filter((action) => matches(action, playbook)); const executionRows = executions.filter((execution) => playbook.executionTypes.includes(execution.executionType as never) && playbookActions.some((action) => action.id === execution.actionId)); const evidence = executionRows.flatMap((execution) => governedExecutionService.listEvidence(tenantId, execution.id)); const missingCapabilities = playbook.capabilities.filter((capability) => !connectors.some((connector) => connectorSupportsCapability(connector, capability))); const linkedArtifacts = artifacts.filter((artifact) => playbookActions.some((action) => action.id === artifact.actionId)); const linkedOutcomeIds = new Set(playbookActions.flatMap((action) => action.outcomeIds)); const linkedOutcomes = outcomes.filter((outcome) => linkedOutcomeIds.has(outcome.id) || linkedArtifacts.some((artifact) => artifact.id === outcome.assetId)); const linkedProtected = protectedOutcomes.filter((outcome) => linkedOutcomeIds.has(outcome.outcomeId) || playbookActions.some((action) => action.id === outcome.actionId) || linkedArtifacts.some((artifact) => artifact.id === outcome.assetId)); const destructivePartial = playbook.executionTypes.some((type) => type === "AZURE_DELETE_MANAGED_DISK" || type === "AZURE_RELEASE_PUBLIC_IP"); const hasRollbackPayload = evidence.some((row) => row.evidenceType === "ROLLBACK_PAYLOAD"); const hasPartialEvidence = evidence.some((row) => row.evidenceType === "ROLLBACK_RESULT" && /PARTIAL/i.test(row.summary)); const rollback: AzureRollbackStatus = hasRollbackPayload ? destructivePartial ? "PARTIAL" : "COMPLETE" : "MISSING"; const base = { playbookId: playbook.playbookId, name: playbook.name, discovery: complete(playbookActions.some((action) => action.evidenceIds.some((id) => /discover|recommendation|azure/i.test(id)))), trust: complete(playbookActions.some((action) => (action.trustScore ?? 0) > 0 && action.evidenceIds.some((id) => /trust|readiness|identity|usage|financial/i.test(id)))), approval: complete(playbookActions.some((action) => ["APPROVED", "VERIFIED", "RETAINED"].includes(action.status) || action.evidenceIds.some((id) => /approval/i.test(id)))), execution: executionRows.some((row) => row.executionMode === "CONTROLLED" && row.status === "COMPLETED" && missingCapabilities.length === 0) ? "CONTROLLED_EXECUTION" as const : executionRows.some((row) => row.executionMode === "SIMULATION" || row.status === "DRY_RUN") ? "SIMULATED_ONLY" as const : "NOT_IMPLEMENTED" as const, rollback, verification: complete(evidence.some((row) => row.evidenceType === "VERIFICATION_RESULT" && /passed/i.test(row.summary))), outcome: complete(linkedOutcomes.length > 0), protection: complete(linkedProtected.length > 0), drift: complete(linkedProtected.some((outcome) => outcome.policyIds.length > 0 && outcome.nextCheckAt)), executiveProof: complete(evidence.some((row) => row.evidenceType === "PRE_STATE") && evidence.some((row) => row.evidenceType === "POST_STATE") && evidence.some((row) => row.evidenceType === "VERIFICATION_RESULT") && linkedProtected.length > 0) }; const rowBlockers = blockers(base, missingCapabilities, !destructivePartial || hasPartialEvidence || hasRollbackPayload); return { ...base, certified: rowBlockers.length === 0, blockers: rowBlockers }; });
  return { certified: playbookStatus.every((row) => row.certified), playbooks: playbookStatus, certifiedPlaybooks: playbookStatus.filter((row) => row.certified).length, uncertifiedPlaybooks: playbookStatus.filter((row) => !row.certified).length };
}
