export const vendorId=(tenantId:string,nameOrId:string)=>`portfolio_vendor::${tenantId}::${String(nameOrId).toLowerCase().replace(/\s+/g,'-')}`;export const productId=(tenantId:string,vendorId:string,name:string)=>`portfolio_product::${tenantId}::${vendorId}::${name.toLowerCase().replace(/\s+/g,'-')}`;export const assetId=(tenantId:string,type:string,source:string)=>`portfolio_asset::${tenantId}::${type}::${String(source).toLowerCase().replace(/\s+/g,'-')}`;export const applicationId=(tenantId:string,name:string)=>`portfolio_application::${tenantId}::${name.toLowerCase().replace(/\s+/g,'-')}`;
