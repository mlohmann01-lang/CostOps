import type { ContractState, ActionContract } from "./execution-contract-types"; import { evaluateExecutionPreconditions } from "./execution-precondition-engine"; export const evaluateExecutionEligibility=(c:ActionContract,i:{evidence:number;confidence:number}):ContractState=>!c.rollbackAvailable?"ROLLBACK_REQUIRED":!evaluateExecutionPreconditions(c,i).ok?"EXECUTION_BLOCKED":c.blastRadius>0.8?"APPROVAL_REQUIRED":"EXECUTION_ELIGIBLE";
