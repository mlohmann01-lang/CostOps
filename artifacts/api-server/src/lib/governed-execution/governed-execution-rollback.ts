import type {GovernedExecutionPlan,GovernedExecutionRollbackPlan} from './governed-execution-types';
export function buildRollbackPlan(p:GovernedExecutionPlan):GovernedExecutionRollbackPlan{const now=new Date().toISOString();const manual=p.actionType==='MANUAL_TASK'||p.actionType.endsWith('REVIEW')||p.actionType==='EVIDENCE_COLLECTION';return {id:`${p.id}-rollback`,tenantId:p.tenantId,planId:p.id,available:!p.destructive||p.reversible,rollbackType:p.destructive?'RESTORE_PREVIOUS_STATE':(manual?'MANUAL':'NO_OP'),steps:[{order:1,title:p.destructive?'Restore previous state':'Record rollback assessment',description:p.destructive?'Manual restoration is required before destructive adapters are enabled.':'Review/manual/evidence tasks do not mutate live state.',manual:true}],limitations:p.destructive&&!p.reversible?['Destructive plan has no rollback and is blocked.']:[],evidenceRefs:p.evidenceRefs,createdAt:now,updatedAt:now}}
