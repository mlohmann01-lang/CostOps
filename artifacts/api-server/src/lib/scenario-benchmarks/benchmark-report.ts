import { runScenarioBenchmark } from './benchmark-runner';import type { Scenario } from './scenario-benchmark-types';export const computeScenarioBenchmarkReport=(input:{scenarios:Scenario[]})=>{const r=runScenarioBenchmark(input); const deviations=r.filter((x,i)=>x.actualGovernanceClass!==input.scenarios[i].expectedGovernanceClass||x.actualDominantRisk!==input.scenarios[i].expectedDominantRisk||x.actualArbitrationResult!==input.scenarios[i].expectedArbitrationResult).map((x)=>x.id); return {results:r,deviations};};
