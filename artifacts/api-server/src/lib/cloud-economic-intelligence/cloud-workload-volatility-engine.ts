import type { CloudResourceEconomicSignal, CloudWorkloadVolatilityResult } from './cloud-economic-types';
export function evaluateCloudWorkloadVolatility(resource:CloudResourceEconomicSignal):CloudWorkloadVolatilityResult { const score=Math.max(0,Math.min(1,((resource.usageVariability??0)*0.25)+((resource.seasonality??0)*0.15)+((resource.burstiness??0)*0.2)+(Math.min(1,(resource.deploymentFrequency??0)/20)*0.15)+((resource.businessCycleDependency??0)*0.1)+((resource.batchIndicator?0.1:0)+(resource.scheduledIndicator?0.05:0)))); const band=score>=0.7?'HIGH':score>=0.4?'MEDIUM':'LOW'; return { volatilityScore:score, volatilityBand:band, volatilityReasons:[resource.scheduledIndicator?'Scheduled workload detected, avoid idle false positives':'No schedule indicators', resource.batchIndicator?'Batch workload indicators present':'No batch indicator'], rightsizingRisk:score, shutdownRisk:Math.max(score,resource.scheduledIndicator||resource.batchIndicator?0.7:score), forecastingRisk:score }; }
