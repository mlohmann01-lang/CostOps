import type { CloudEnvironmentLifecycleResult, CloudResourceEconomicSignal } from './cloud-economic-types';
export function evaluateCloudEnvironmentLifecycle(resource:CloudResourceEconomicSignal):CloudEnvironmentLifecycleResult { const devLike=resource.environmentType==='DEV'||resource.environmentType==='TEST'||resource.environmentType==='SANDBOX'; const alwaysOn=(resource.activeHours??0)>160; const ttlGap=resource.tags?.ttl?false:true; const score=Math.min(1,(devLike?0.35:0)+(alwaysOn?0.35:0)+(ttlGap?0.3:0)); const savings=(resource.monthlyCost||0)*(devLike&&alwaysOn?0.45:0.15); return { lifecycleWasteScore:score, ttlPolicyRecommended:ttlGap, shutdownScheduleRecommended:devLike&&alwaysOn, ownerReviewRequired:!resource.ownerId, estimatedMonthlySavings:savings, recurrencePreventionPolicyHint:'Enforce environment TTL tags and weekend shutdown review.', confidence:devLike?0.8:0.6 }; }
