import type { CloudResourceEconomicSignal, CloudStorageLifecycleEconomics } from './cloud-economic-types';
export function evaluateCloudStorageLifecycleEconomics(resource:CloudResourceEconomicSignal):CloudStorageLifecycleEconomics { const stale=resource.tags?.staleSnapshot==='true'; const unattached=resource.tags?.unattachedVolume==='true'; const cold=resource.tags?.coldObjectStorage==='true'; const oversized=resource.tags?.oversizedDatabaseStorage==='true'; const missingPolicy=resource.tags?.lifecyclePolicyMissing==='true'; const backupSprawl=resource.tags?.backupRetentionSprawl==='true'; const score=[stale,unattached,cold,oversized,missingPolicy,backupSprawl].filter(Boolean).length/6; return { storageWasteScore:score, lifecyclePolicyRecommended:missingPolicy||cold||backupSprawl, archiveRecommended:cold, deletionReviewRecommended:stale||unattached, estimatedMonthlyWaste:(resource.monthlyCost||0)*score*0.6, retentionRisk:backupSprawl?0.8:0.3, confidence:0.75 }; }
