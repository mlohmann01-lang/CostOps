import type { FinancialTruthCollection, PersistenceStore } from './financial-truth-types';
const matches=<T extends Record<string, any>>(v:T,f:Record<string,unknown>={})=>Object.entries(f).every(([k,val])=>val===undefined||String(v[k])===String(val));
export class MemoryPersistenceStore<T extends {id:string;tenantId:string}> implements PersistenceStore<T>{ private rows=new Map<string,T>(); constructor(readonly collection:FinancialTruthCollection){} private key(t:string,id:string){return `${t}:${id}`} async upsert(v:T){this.rows.set(this.key(v.tenantId,v.id),v);return v} async get(t:string,id:string){return this.rows.get(this.key(t,id))} async list(t:string,f:Record<string,unknown>={}){return [...this.rows.values()].filter(v=>v.tenantId===t&&matches(v as any,f))} async deleteTenant(t:string){for(const v of this.rows.values()) if(v.tenantId===t)this.rows.delete(this.key(t,v.id))} async size(){return this.rows.size}}
export class DatabasePersistenceStore<T extends {id:string;tenantId:string}> implements PersistenceStore<T>{ constructor(readonly collection:FinancialTruthCollection){} private pk(v:{tenantId:string;id:string}){return `${v.tenantId}:${this.collection}:${v.id}`} private async db(){return import('@workspace/db')} async upsert(v:T){const { db, financialTruthAuthorityRecordsTable }=await this.db(); await db.insert(financialTruthAuthorityRecordsTable).values({id:this.pk(v),tenantId:v.tenantId,collection:this.collection,recordId:v.id,payload:v}).onConflictDoUpdate({target:financialTruthAuthorityRecordsTable.id,set:{payload:v,updatedAt:new Date()}}); return v} async get(t:string,id:string){const { db, financialTruthAuthorityRecordsTable }=await this.db(); const { and, eq }=await import('drizzle-orm'); const rows=await db.select().from(financialTruthAuthorityRecordsTable).where(and(eq(financialTruthAuthorityRecordsTable.tenantId,t),eq(financialTruthAuthorityRecordsTable.collection,this.collection),eq(financialTruthAuthorityRecordsTable.recordId,id))).limit(1); return rows[0]?.payload as T|undefined} async list(t:string,f:Record<string,unknown>={}){const { db, financialTruthAuthorityRecordsTable }=await this.db(); const { and, eq }=await import('drizzle-orm'); const rows=await db.select().from(financialTruthAuthorityRecordsTable).where(and(eq(financialTruthAuthorityRecordsTable.tenantId,t),eq(financialTruthAuthorityRecordsTable.collection,this.collection))); return rows.map(r=>r.payload as T).filter(v=>matches(v as any,f))} async deleteTenant(t:string){const { db, financialTruthAuthorityRecordsTable }=await this.db(); const { and, eq }=await import('drizzle-orm'); await db.delete(financialTruthAuthorityRecordsTable).where(and(eq(financialTruthAuthorityRecordsTable.tenantId,t),eq(financialTruthAuthorityRecordsTable.collection,this.collection)))} async size(){const { db, financialTruthAuthorityRecordsTable }=await this.db(); const { eq }=await import('drizzle-orm'); return (await db.select().from(financialTruthAuthorityRecordsTable).where(eq(financialTruthAuthorityRecordsTable.collection,this.collection))).length}}
export function createPersistenceStore<T extends {id:string;tenantId:string}>(collection:FinancialTruthCollection):PersistenceStore<T>{return process.env.DATABASE_URL?new DatabasePersistenceStore<T>(collection):new MemoryPersistenceStore<T>(collection)}
