import type { RecommendationRealizationInput, RecommendationRealizationResult } from './calibration-types';
export function calibrateRecommendationRealization(inputs:RecommendationRealizationInput[]):{ score:number; results:RecommendationRealizationResult[] } { if(inputs.length===0){ return { score:1, results:[] }; } const results=inputs.map((i)=>{ const approvalHit=(i.forecastedApprovalProbability>=0.5)===i.approved; const reversalMiss=i.forecastedApprovalProbability>=0.7&&i.reversed; return { ...i, approvalHit, reversalMiss, savingsError:Math.abs(i.forecastedSavings-i.realizedSavings) }; }); const approvalRate=results.filter((r)=>r.approvalHit).length/results.length; const reversalPenalty=results.filter((r)=>r.reversalMiss).length/results.length; const savingsPenalty=Math.min(1,results.reduce((s,r)=>s+r.savingsError,0)/(results.length*100)); return { score:Math.max(0,approvalRate*0.5+(1-reversalPenalty)*0.3+(1-savingsPenalty)*0.2), results }; }
