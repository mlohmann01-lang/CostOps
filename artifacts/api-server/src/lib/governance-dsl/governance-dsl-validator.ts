import type { DSLAst } from './governance-dsl-types';const allowedTargets=['READ_ONLY','RECOMMEND_ONLY','APPROVAL_REQUIRED','BLOCKED','AGGRESSIVE_OPTIMIZATION'];export const validateGovernanceDSL=(ast:DSLAst)=>({valid:ast.rules.length>0&&ast.rules.every((r)=>allowedTargets.includes(r.target)),errors:ast.rules.filter((r)=>!allowedTargets.includes(r.target)).map((r)=>`unknown action target:${r.target}`)});
