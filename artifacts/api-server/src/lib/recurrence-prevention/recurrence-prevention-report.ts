import type {RecurrencePreventionInput} from './recurrence-prevention-types';import {evaluateRecurrenceControls} from './recurrence-control-engine';import {evaluateGovernancePersistence} from './governance-persistence-engine';import {evaluateRecurrenceRiskDecay} from './recurrence-risk-decay';import {evaluatePolicyCoverage} from './recurrence-policy-coverage';import {computeRecurrencePreventionScore} from './recurrence-prevention-score';export const computeRecurrencePreventionReport=(i:RecurrencePreventionInput)=>({controls:evaluateRecurrenceControls(i),persistence:evaluateGovernancePersistence(i),riskDecay:evaluateRecurrenceRiskDecay(i),policyCoverage:evaluatePolicyCoverage(i),score:computeRecurrencePreventionScore(i)});