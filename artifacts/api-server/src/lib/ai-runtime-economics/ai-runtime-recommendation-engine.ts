import type { AIRuntimeEconomicSignal } from './ai-runtime-economic-types';
export function generateAIRuntimeEconomicRecommendations(s:AIRuntimeEconomicSignal){const out:string[]=[]; if(s.retryRate>0.25) out.push('RETRY_WASTE_REVIEW','TOKEN_COST_OPTIMIZATION_REVIEW'); if(s.cacheHitRate<0.4) out.push('CACHE_EFFICIENCY_REVIEW'); if(s.idleCapacity>0.3) out.push('GPU_IDLE_REVIEW'); if((1-s.batchability)*s.concurrency>0.6) out.push('GPU_FRAGMENTATION_REVIEW'); if(s.workloadType==='AGENT_RUNTIME') out.push('AGENT_LOOP_COST_REVIEW'); if(s.workloadType==='TRAINING'&&s.burstVolatility>0.45) out.push('TRAINING_WORKLOAD_REVIEW'); return [...new Set(out)];}
