import type { EconomicForecastWindow, RecommendationRealizationForecast } from './economic-forecasting-types';
export interface RecommendationRealizationInput { tenantId:string; recommendationId:string; forecastWindow:EconomicForecastWindow; historicalApprovalRate:number; overrideRate:number; operationalSensitivity:number; governanceRisk:number; recommendationConfidence:number; departmentFollowThrough:number; evidenceRefs?:string[]; assumptions?:string[]; }
const c=(n:number)=>Math.max(0,Math.min(1,n));
export function forecastRecommendationRealization(input:RecommendationRealizationInput):RecommendationRealizationForecast { const approval=c(input.historicalApprovalRate*0.4+input.recommendationConfidence*0.4+input.departmentFollowThrough*0.2-input.operationalSensitivity*0.2); const reversal=c(input.overrideRate*0.5+(1-input.recommendationConfidence)*0.5); return { tenantId:input.tenantId,recommendationId:input.recommendationId,forecastWindow:input.forecastWindow,approvalLikelihood:approval,reversalLikelihood:reversal,projectedRealizationRate:c(approval*(1-reversal)),projectedExecutionDelay:c(input.operationalSensitivity*0.7+input.governanceRisk*0.3),projectedRollbackRisk:c(input.governanceRisk*0.6+input.overrideRate*0.4),confidence:c((input.recommendationConfidence+input.historicalApprovalRate)/2*(1-input.overrideRate*0.3)),evidenceRefs:input.evidenceRefs??[],assumptions:input.assumptions??[] }; }
