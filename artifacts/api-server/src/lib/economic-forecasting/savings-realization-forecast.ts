import type { SavingsRealizationForecast } from './economic-forecasting-types';
export interface SavingsRealizationInput { tenantId:string; recommendationId:string; projectedSavings:number; recommendationConfidence:number; historicalReversals:number; overrideRate:number; departmentBehaviorRisk:number; ownershipClarity:number; operationalSensitivity:number; duplicateAttributionRisk:number; governanceRisk:number; evidenceRefs?:string[]; assumptions?:string[]; }
const c=(n:number)=>Math.max(0,Math.min(1,n));
export function forecastSavingsRealization(input:SavingsRealizationInput):SavingsRealizationForecast { const realization=c(input.recommendationConfidence*(1-input.historicalReversals*0.5)*(1-input.overrideRate*0.4)*(1-input.duplicateAttributionRisk*0.5)); const reversal=c((1-input.recommendationConfidence)*0.5+input.historicalReversals*0.3+input.overrideRate*0.2); const conf=c(realization*(1-input.governanceRisk*0.3)*(0.5+input.ownershipClarity*0.5)); return { tenantId:input.tenantId,recommendationId:input.recommendationId,projectedSavings:input.projectedSavings*realization,realizationProbability:realization,reversalProbability:reversal,confidence:conf,evidenceRefs:input.evidenceRefs??[],assumptions:input.assumptions??[],projectedSavingsRealizationRate:realization,projectedReversalRate:reversal,projectedFalsePositiveRisk:c(input.duplicateAttributionRisk*0.6+(1-input.recommendationConfidence)*0.4),projectedGovernanceFriction:c(input.governanceRisk*0.6+input.operationalSensitivity*0.4) }; }
