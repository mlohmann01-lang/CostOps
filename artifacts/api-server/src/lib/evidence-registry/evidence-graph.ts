export const EVIDENCE_GRAPH_NODE_TYPES=['EVIDENCE_RECORD','EVIDENCE_ARTIFACT','EVIDENCE_LINK','EVIDENCE_INTEGRITY_CHECK','EVIDENCE_RETENTION_POLICY','EVIDENCE_REDACTION_RECORD','EVIDENCE_EXPORT_MARKER'] as const;
export const EVIDENCE_GRAPH_EDGE_TYPES=['SUPPORTED_BY_EVIDENCE','VALIDATES','PROVES','DERIVED_FROM','SUPERSEDES','GOVERNED_BY','HAS_RISK','EXPORTS'] as const;
export class EvidenceGraphService{events:any[]=[];constructor(private graph?:any){}async linkRecord(record:any,related:any={}){try{await this.graph?.createNode?.({type:'EVIDENCE_RECORD',id:record.evidenceRef,tenantId:record.tenantId,payload:record});for(const a of related.artifacts??[])await this.graph?.createEdge?.({type:'SUPPORTED_BY_EVIDENCE',from:record.evidenceRef,to:a.id});for(const l of related.links??[])await this.graph?.createEdge?.({type:l.relationshipType==='PROVES'?'PROVES':'VALIDATES',from:record.evidenceRef,to:l.targetId});return {status:'PASS'}}catch(error:any){this.events.push({eventType:'EVIDENCE_REGISTRY_GRAPH_LINK_FAILED',error:error.message});return {status:'WARN',eventType:'EVIDENCE_REGISTRY_GRAPH_LINK_FAILED'}}}}
