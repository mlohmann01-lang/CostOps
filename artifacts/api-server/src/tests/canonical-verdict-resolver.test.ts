import test from "node:test"; import assert from "node:assert/strict"; import * as m from "../lib/runtime-verdict-continuity/canonical-verdict-resolver"; test("canonical-verdict-resolver",()=>{ assert.equal(Object.values(m).length>0,true); });