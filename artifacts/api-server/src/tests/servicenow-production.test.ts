import test from'node:test';import assert from'node:assert/strict';import{ServiceNowProductionService,createServiceNowFetchFixture}from'../lib/production-connectors/servicenow';
test('ServiceNow live safety, tenant isolation, findings and summary',async()=>{const svc=new ServiceNowProductionService({fetchImpl:createServiceNowFetchFixture() as any});assert.equal((await svc.connect({tenantId:'bad',instanceUrl:'https://mock.service-now.com',credentials:{authType:'API_TOKEN',apiToken:'demo'},mode:'LIVE'})).status,'FAIL');await svc.connect({tenantId:'a',instanceUrl:'https://tenant.service-now.com',credentials:{authType:'API_TOKEN',apiToken:'token'},mode:'LIVE'});await svc.discover('a');assert.equal(svc.getSummary('a').applicationCount,2);assert.equal(svc.getSummary('b').applicationCount,0);});
