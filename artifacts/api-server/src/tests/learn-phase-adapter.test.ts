import test from "node:test"; import assert from "node:assert/strict"; import * as m from "../lib/runtime-phase-adapters/learn-phase-adapter"; test("learn-phase-adapter",()=>{ assert.equal(Object.values(m).length>0,true); });