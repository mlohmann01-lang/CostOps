import test from'node:test';import assert from'node:assert/strict';import{ProcurementApProductionService,createProcurementApFetchFixture}from'../lib/production-connectors/procurement-ap';
test('Procurement/AP evidence generated',async()=>{const svc=new ProcurementApProductionService({fetchImpl:createProcurementApFetchFixture() as any});await svc.connect({tenantId:'pape',profileKey:'COUPA',baseUrl:'https://proc.example.com',credentials:{authType:'API_TOKEN',apiToken:'token'},mode:'LIVE'});await svc.discover('pape');assert.ok((await (svc as any).deps.evidence.listEvidenceRecords('pape')).length>0);assert.ok((await (svc as any).deps.evidence.listArtifacts('pape')).length>0);});
