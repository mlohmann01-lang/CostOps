import test from "node:test";import assert from "node:assert/strict";import { evaluateExecutionEligibility } from "../lib/governed-execution-contracts"; test("no rollback => no execution",()=>{const s=evaluateExecutionEligibility({actionType:"a",blastRadius:0.2,rollbackAvailable:false,reversibility:1,requiredEvidence:1,requiredConfidence:0.5,requiredApprovalTier:1,executionWindow:"w",expirationWindow:"e",governanceRequirements:[]},{evidence:1,confidence:1});assert.equal(s,"ROLLBACK_REQUIRED");});