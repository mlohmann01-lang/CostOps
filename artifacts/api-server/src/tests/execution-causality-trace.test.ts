import test from "node:test"; import assert from "node:assert/strict"; import * as x from "../lib/execution-forensics/execution-causality-trace"; test("execution-causality-trace",()=>{ assert.equal(Object.values(x).length>0,true); });