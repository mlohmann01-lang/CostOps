import test from 'node:test'; import assert from 'node:assert/strict'; import { calculatePlaybookReliability, recordCalibrationMemory } from '../lib/economic-memory';
test('playbook reliability scores',()=>{ const rs=[recordCalibrationMemory({tenantId:'t',sourceType:'PLAYBOOK',sourceId:'1',domain:'M365',entityId:'e',playbookId:'p1',forecastId:'f',scenarioId:'s',recommendationId:'r',calibrationType:'SAVINGS',expectedValue:100,realizedValue:95,confidenceBefore:0.7,confidenceAfter:0.72,evidenceRefs:['a'],replayCorrelationId:'x',lineageCorrelationId:'y'})]; const out=calculatePlaybookReliability(rs); assert.equal(out[0].playbookId,'p1'); });