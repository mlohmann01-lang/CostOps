import test from "node:test"; import assert from "node:assert/strict"; import fs from "node:fs"; import { resolve } from "node:path";
test("adobe telemetry events catalog present", ()=>{ const c=fs.readFileSync(resolve(process.cwd(),"src/lib/observability/operational-telemetry-service.ts"),"utf8"); assert.equal(c.includes("REQUIRED_ADOBE_RUNTIME_EVENTS"), true); assert.equal(c.includes("emitAdobeEvent"), true); });
