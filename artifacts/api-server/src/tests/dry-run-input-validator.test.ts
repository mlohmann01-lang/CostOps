import test from 'node:test';import assert from 'node:assert/strict';import {validateDryRunInput} from '../lib/governed-dry-run/dry-run-input-validator';test('missing lineage invalid',()=>{assert.equal(validateDryRunInput({actionCategory:'CLOUD_RIGHTSIZE_SIMULATION',evidenceIntegrity:80,lineageIntegrity:0,blastRadius:40,reversible:true,production:false,domain:'CLOUD'}).valid,false);});