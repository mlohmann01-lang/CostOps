import test from "node:test"; import assert from "node:assert/strict"; import * as m from "../lib/canonical-runtime-orchestration/canonical-runtime-report"; test("canonical-runtime-report",()=>{ assert.equal(Object.values(m).length>0,true); });