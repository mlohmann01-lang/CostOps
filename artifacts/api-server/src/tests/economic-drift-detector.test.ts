import test from 'node:test'; import assert from 'node:assert/strict'; import { detectEconomicDrift } from '../lib/economic-memory-drift'; test('detect drift',()=>{assert.ok(detectEconomicDrift([{tenantId:'t',domain:'CLOUD',category:'x',timestamp:'2026-01-01',savingsDelta:-1,evidenceReferences:[{id:'1',source:'s',lineageId:'l',replayId:'r',confidence:1,capturedAt:'t'}],approved:true}]).includes('SAVINGS_DECAY'));});
