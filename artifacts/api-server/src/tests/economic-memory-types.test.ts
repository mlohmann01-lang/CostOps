import test from 'node:test'; import assert from 'node:assert/strict'; import type { EconomicMemoryRecord } from '../lib/economic-memory';
test('economic memory types shape compile',()=>{ const rec:EconomicMemoryRecord={tenantId:'t',sourceType:'FORECAST',sourceId:'s',domain:'M365',entityId:'e',playbookId:'p',forecastId:'f',scenarioId:'sc',recommendationId:'r',calibrationType:'SPEND',expectedValue:100,realizedValue:90,errorRate:0.1,biasDirection:'OVER_ESTIMATION',confidenceBefore:0.7,confidenceAfter:0.65,evidenceRefs:['ev'],replayCorrelationId:'rep',lineageCorrelationId:'lin'}; assert.equal(rec.tenantId,'t'); });