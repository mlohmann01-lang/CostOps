import test from "node:test"; import assert from "node:assert/strict"; import * as m from "../lib/runtime-phase-adapters/discover-phase-adapter"; test("discover-phase-adapter",()=>{ assert.equal(Object.values(m).length>0,true); });