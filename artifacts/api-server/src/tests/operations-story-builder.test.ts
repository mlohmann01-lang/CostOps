import test from 'node:test';import assert from 'node:assert/strict';import { operationsstorybuilder } from '../lib/runtime-narrative-engine/runtime-story-builder';
test('story includes provider-specific risk context',()=>{ const s=operationsstorybuilder({provider:'M365',propagationRisk:'HIGH',dependencyRisk:'HIGH',verificationDelay:'MEDIUM',rollbackTiming:'DELAYED',sharedService:true,approval:'SECURITY_REVIEW',blastRadius:'ELEVATED'}); assert.ok(s.includes('propagation HIGH')); assert.ok(s.includes('verification delay MEDIUM')); assert.ok(s.includes('blast-radius ELEVATED'));});
