import test from "node:test"; import assert from "node:assert/strict"; import * as x from "../lib/execution-verdict-engine/execution-gating-reason-builder"; test("execution-gating-reason-builder",()=>{ assert.equal(Object.values(x).length>0,true); });