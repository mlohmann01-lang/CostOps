import test from "node:test"; import assert from "node:assert/strict"; import * as m from "../lib/runtime-proof-continuity/execution-proof-continuity"; test("execution-proof-continuity",()=>{ assert.equal(Object.values(m).length>0,true); });