import test from 'node:test'; import assert from 'node:assert/strict'; import { calculateDomainReliability, recordCalibrationMemory } from '../lib/economic-memory';
test('domain reliability includes canonical domains',()=>{ const rs=[recordCalibrationMemory({tenantId:'t',sourceType:'DOMAIN',sourceId:'1',domain:'M365',entityId:'e',playbookId:'p',forecastId:'f',scenarioId:'s',recommendationId:'r',calibrationType:'SPEND',expectedValue:100,realizedValue:90,confidenceBefore:0.7,confidenceAfter:0.65,evidenceRefs:['z'],replayCorrelationId:'x',lineageCorrelationId:'y'})]; const out=calculateDomainReliability(rs); assert.equal(out.length,5); });