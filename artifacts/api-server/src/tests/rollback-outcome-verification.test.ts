import test from "node:test"; import assert from "node:assert/strict"; import * as m from "../lib/execution-replay-governance/rollback-outcome-verification"; test("rollback-outcome-verification",()=>{ assert.equal(Object.values(m).length>0,true); });