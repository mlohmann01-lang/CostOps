import test from'node:test';import assert from'node:assert/strict';import{ProcurementApProductionService,createProcurementApFetchFixture}from'../lib/production-connectors/procurement-ap';
test('Procurement/AP graph nodes and edges generated',async()=>{const graph={nodes:[] as any[],edges:[] as any[],createNode:async(x:any)=>graph.nodes.push(x),createEdge:async(x:any)=>graph.edges.push(x)};const svc=new ProcurementApProductionService({fetchImpl:createProcurementApFetchFixture() as any,graph});await svc.connect({tenantId:'papg',profileKey:'COUPA',baseUrl:'https://proc.example.com',credentials:{authType:'API_TOKEN',apiToken:'token'},mode:'LIVE'});await svc.discover('papg');assert.ok(graph.nodes.length>0);assert.ok(graph.edges.length>0);});
