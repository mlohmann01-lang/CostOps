import test from 'node:test';import assert from 'node:assert/strict';import {modelDryRunAction} from '../lib/governed-dry-run/dry-run-action-model';test('dry run action always non-executable',()=>{assert.equal(modelDryRunAction({actionCategory:'M365_LICENSE_REMOVE_SIMULATION',evidenceIntegrity:90,lineageIntegrity:90,blastRadius:10,reversible:true,production:false,domain:'M365'}).executable,false);});