import test from 'node:test'; import assert from 'node:assert/strict'; import fs from 'node:fs';
test('runtime boundaries preserved',()=>{ const a=fs.readFileSync(new URL('../lib/runtime-hardening/runtime-hardening-phase-a.ts', import.meta.url),'utf8'); const c=fs.readFileSync(new URL('../lib/runtime-hardening/sustained-runtime-load-phase-c.ts', import.meta.url),'utf8'); ['execution-engine','auto-approval','mutation','autonomous'].forEach(k=>{assert.equal(a.includes(k),false);assert.equal(c.includes(k),false);});});
