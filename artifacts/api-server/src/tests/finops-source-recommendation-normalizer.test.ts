import test from 'node:test'; import assert from 'node:assert/strict'; import { normalizeFinOpsSourceRecommendation } from '../lib/finops-operationalization';
test('normalizes finops recommendation',()=>{ const n=normalizeFinOpsSourceRecommendation({tenantId:'t',sourceSystem:'FLEXERA',sourceRecommendationId:'1',cloudProvider:'AWS',recommendationCategory:'IDLE_RESOURCE',recommendationSummary:'idle',estimatedMonthlySavings:100,savingsConfidence:0.8,ownerHints:['o'],costCenterHints:['c'],evidenceRefs:['e1','e2'],sourceCreatedAt:'a',sourceUpdatedAt:'b',lineageCorrelationId:'l',replayCorrelationId:'r'}); assert.equal(n.estimatedAnnualSavings,1200); assert.equal(n.sourceReliability,'HIGH'); });