import test from 'node:test'; import assert from 'node:assert/strict'; import { evaluateSavingsDegradation } from '../lib/economic-memory-drift'; test('degradation',()=>{assert.ok(evaluateSavingsDegradation([{tenantId:'t',domain:'CLOUD',category:'x',timestamp:'2026-01-01',savingsDelta:-1,evidenceReferences:[{id:'1',source:'s',lineageId:'l',replayId:'r',confidence:1,capturedAt:'t'}],approved:true}]).degradation>0);});
