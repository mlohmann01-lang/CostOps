import test from'node:test';import assert from'node:assert/strict';import{ServiceNowProductionService,createServiceNowFetchFixture}from'../lib/production-connectors/servicenow';
test('ServiceNow evidence records and artifacts generated',async()=>{const svc=new ServiceNowProductionService({fetchImpl:createServiceNowFetchFixture() as any});await svc.connect({tenantId:'snowe',instanceUrl:'https://tenant.service-now.com',credentials:{authType:'API_TOKEN',apiToken:'token'},mode:'LIVE'});await svc.discover('snowe');assert.ok((await (svc as any).deps.evidence.listEvidenceRecords('snowe')).length>=2);assert.ok((await (svc as any).deps.evidence.listArtifacts('snowe')).length>=2);});
