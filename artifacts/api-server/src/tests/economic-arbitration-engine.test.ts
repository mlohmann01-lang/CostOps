import test from 'node:test'; import assert from 'node:assert/strict'; import { computeEconomicArbitration } from '../lib/economic-arbitration'; test('arbitration recommendation only',()=>{const out=computeEconomicArbitration([{id:'a',savings:100,governanceRisk:0.1,volatilityRisk:0.8,recurrenceRisk:0,confidence:0.9,severity:0.6},{id:'b',savings:60,governanceRisk:0.9,volatilityRisk:0.2,recurrenceRisk:0,confidence:0.7,severity:0.8}]); assert.equal('executionPayload' in out,false);});
