import test from "node:test"; import assert from "node:assert/strict"; import * as m from "../lib/execution-lineage/execution-evidence-chain"; test("execution-evidence-chain",()=>{ assert.equal(Object.values(m).length>0,true); });