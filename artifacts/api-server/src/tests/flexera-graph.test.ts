import test from 'node:test';import assert from 'node:assert/strict';import {FlexeraProductionService,createFlexeraFetchFixture} from '../lib/production-connectors/flexera';
test('Flexera discovery creates economic graph nodes and edges',async()=>{const graph={nodes:0,edges:0,createNode:async()=>{graph.nodes++;return{}},createEdge:async()=>{graph.edges++;return{}}};const svc=new FlexeraProductionService({fetchImpl:createFlexeraFetchFixture() as any,graph});await svc.connect({tenantId:'t',apiBaseUrl:'https://flexera.example',credentials:{authType:'API_TOKEN',apiToken:'token'},mode:'LIVE'});await svc.discover('t');assert.ok(graph.nodes>=5);assert.ok(graph.edges>=4);});
