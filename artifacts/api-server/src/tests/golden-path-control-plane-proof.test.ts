import test from 'node:test';import assert from 'node:assert/strict';import { computeGoldenPathControlPlaneProof } from '../lib/golden-path/golden-path-control-plane-proof';test('control plane proof deterministic',()=>{const a=computeGoldenPathControlPlaneProof();const b=computeGoldenPathControlPlaneProof();assert.equal(a.proofHash,b.proofHash);assert.equal(a.boundaryProof.ok,true);});