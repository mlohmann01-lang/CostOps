import test from'node:test';import assert from'node:assert/strict';import{ErpProductionService,createErpFetchFixture}from'../lib/production-connectors/erp';
test('ERP graph nodes and edges generated',async()=>{const graph={nodes:[] as any[],edges:[] as any[],createNode:async(x:any)=>graph.nodes.push(x),createEdge:async(x:any)=>graph.edges.push(x)};const svc=new ErpProductionService({fetchImpl:createErpFetchFixture() as any,graph});await svc.connect({tenantId:'erpg',profileKey:'SAP',baseUrl:'https://erp.example.com',credentials:{authType:'API_TOKEN',apiToken:'token'},mode:'LIVE'});await svc.discover('erpg');assert.ok(graph.nodes.length>0);assert.ok(graph.edges.length>0);});
