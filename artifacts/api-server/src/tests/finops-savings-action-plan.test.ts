import test from 'node:test'; import assert from 'node:assert/strict'; import { buildFinOpsSavingsActionPlan, normalizeFinOpsSourceRecommendation } from '../lib/finops-operationalization';
test('builds governed action plan',()=>{ const a=buildFinOpsSavingsActionPlan(normalizeFinOpsSourceRecommendation({tenantId:'t',sourceSystem:'AWS',sourceRecommendationId:'1',cloudProvider:'AWS',recommendationCategory:'COMMITMENT_OPTIMIZATION',recommendationSummary:'commit',estimatedMonthlySavings:200,savingsConfidence:0.4,sourceCreatedAt:'a',sourceUpdatedAt:'b',lineageCorrelationId:'l',replayCorrelationId:'r'})); assert.equal(a.recommendedMode,'APPROVAL_REQUIRED'); });