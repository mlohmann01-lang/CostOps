import test from 'node:test';import assert from 'node:assert/strict';import {evaluateDryRunGovernance} from '../lib/governed-dry-run/dry-run-governance-check';test('oracle java review-only enforcement',()=>{assert.equal(evaluateDryRunGovernance({actionCategory:'ORACLE_JAVA_REVIEW_ONLY_SIMULATION',evidenceIntegrity:80,lineageIntegrity:80,blastRadius:50,reversible:false,production:true,domain:'ORACLE_JAVA'}).allowed,true);assert.equal(evaluateDryRunGovernance({actionCategory:'CLOUD_RIGHTSIZE_SIMULATION',evidenceIntegrity:80,lineageIntegrity:80,blastRadius:50,reversible:false,production:true,domain:'ORACLE_JAVA'}).allowed,false);});