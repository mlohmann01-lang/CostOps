import test from 'node:test';import assert from 'node:assert/strict';import {computeDryRunRiskReport} from '../lib/governed-dry-run/dry-run-risk-report';test('report has governance and impact',()=>{const r=computeDryRunRiskReport({actionCategory:'SAAS_LICENSE_RECLAIM_SIMULATION',evidenceIntegrity:80,lineageIntegrity:80,blastRadius:25,reversible:true,production:false,domain:'SAAS'});assert.equal(typeof r.impact.risk,'number');assert.equal(typeof r.governance.allowed,'boolean');});