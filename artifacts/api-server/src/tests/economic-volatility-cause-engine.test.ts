import test from 'node:test'; import assert from 'node:assert/strict'; import { evaluateEconomicVolatilityCause } from '../lib/economic-causality'; test('vol cause',()=>{const out=evaluateEconomicVolatilityCause([{category:'WORKLOAD_VOLATILITY',strength:0.6,evidenceReferences:[{id:'1',source:'s',lineageId:'l',replayId:'r',confidence:1,capturedAt:'x'}],domains:['AI_RUNTIME']}]); assert.equal(out.category,'WORKLOAD_VOLATILITY');});
