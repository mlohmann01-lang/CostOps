import test from 'node:test';import assert from 'node:assert/strict';import { evaluateOracleProcessorRisk } from '../lib/oracle-java-governance';import { buildOracleJavaSignalFixture } from './_oracle-java-test-fixture';test('evaluateOracleProcessorRisk',()=>{const r=evaluateOracleProcessorRisk(buildOracleJavaSignalFixture());assert.ok(r.confidence<=1);});