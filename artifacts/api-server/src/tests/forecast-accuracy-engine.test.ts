import test from 'node:test'; import assert from 'node:assert/strict'; import { evaluateForecastAccuracy } from '../lib/calibration-realism'; test('stable forecast improves confidence',()=>{assert.ok(evaluateForecastAccuracy([1,1,1])>evaluateForecastAccuracy([50,50,50]));});
