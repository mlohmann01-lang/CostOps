import test from 'node:test';import assert from 'node:assert/strict';import {computeGovernedDryRunReport} from '../lib/governed-dry-run/governed-dry-run-report';test('governed dry run report deterministic',()=>{const i={actionCategory:'M365_LICENSE_REMOVE_SIMULATION',evidenceIntegrity:90,lineageIntegrity:90,blastRadius:10,reversible:true,production:false,domain:'M365'} as const;assert.deepEqual(computeGovernedDryRunReport(i),computeGovernedDryRunReport(i));});