import test from "node:test";import assert from "node:assert/strict";import * as m from "../lib/calibration-drift-governance/benchmark-staleness-engine";test("benchmark-staleness-engine",()=>{assert.ok(Object.values(m).length>0);});