import test from 'node:test'; import assert from 'node:assert/strict'; import { assertRecommendationIntegrity } from '../lib/semantic-hardening'; test('recommendation shared contract',()=>{assert.doesNotThrow(()=>assertRecommendationIntegrity({id:'r',domain:'CLOUD',title:'Review',reviewMode:'RECOMMEND_ONLY',evidence:[{id:'1',source:'x',capturedAt:'t',confidence:1,lineage:{lineageId:'l',sourceSystem:'s',entityId:'e'},replay:{replayId:'r',timestamp:'t',version:'1'}}]}));});
