import test from 'node:test';import assert from 'node:assert/strict';import { semanticWeightRegistry } from '../lib/semantic-weight-registry/semantic-weight-registry';test('semantic weight registry has explicit weights',()=>{assert.ok(semanticWeightRegistry.length>0);assert.equal(semanticWeightRegistry[0].semanticDomain,'PROVIDER_PROPAGATION');assert.ok(semanticWeightRegistry[0].explanation.length>0);});
