import test from 'node:test';import assert from 'node:assert/strict';import { buildConnectorActionSemanticsReport } from '../lib/connector-action-semantics/connector-action-semantics-report';test('manual only actions cannot be live eligible',()=>{ const r=buildConnectorActionSemanticsReport(); assert.ok(r.manualOnly>0); assert.equal(r.hasKubernetesMutation,false); assert.ok(r.highRisk>=r.manualOnly);});
