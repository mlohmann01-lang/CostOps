import test from 'node:test'; import assert from 'node:assert/strict'; import { simulateReplayGrowth } from '../lib/runtime-hardening/sustained-runtime-load-phase-c';
test('replay growth simulation',()=>{const r=simulateReplayGrowth({recommendationsPerTenantPerDay:50,averageTransitionsPerRecommendation:8,averageTelemetryEventsPerRecommendation:15,averageWorkflowEventsPerRecommendation:4,durationDays:45,tenantCount:40,missingLineageRate:0.2});assert.equal(r.replayDegradationRisk,'MEDIUM');assert.equal(r.projectedReplayRecords>0,true);});
