import test from "node:test";import assert from "node:assert/strict";import * as m from "../lib/savings-persistence-benchmarking/savings-persistence-score";test("savings-persistence-score",()=>{assert.ok(Object.values(m).length>0);});