import test from "node:test"; import assert from "node:assert/strict"; import fs from "node:fs"; import { resolve } from "node:path";
test("replay service has adobe event emit capability", ()=>{ const c=fs.readFileSync(resolve(process.cwd(),"src/lib/observability/operational-telemetry-service.ts"),"utf8"); assert.equal(c.includes("ADOBE_REPLAY_VALIDATED"), true); assert.equal(c.includes("ADOBE_REPLAY_MISMATCH"), true); });
