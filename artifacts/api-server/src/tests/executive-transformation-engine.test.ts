import test from 'node:test'; import assert from 'node:assert/strict'; import { assessExecutiveTransformationTrajectory } from '../lib/executive-economic-governance'; test('assesses transformation trajectory',()=>{ const x=assessExecutiveTransformationTrajectory({transformations:['AI_TRANSFORMATION'],governanceReadiness:0.7,organizationalFriction:0.4,forecastConfidence:0.7,dependencyStrength:0.7,riskPressure:0.3}); assert.equal(x[0].transformationType,'AI_TRANSFORMATION'); });