import test from 'node:test'; import assert from 'node:assert/strict'; import { evaluateEconomicWasteCause } from '../lib/economic-causality'; test('waste cause',()=>{const out=evaluateEconomicWasteCause([{category:'AUTOSCALING_INEFFICIENCY',strength:0.9,evidenceReferences:[{id:'1',source:'s',lineageId:'l',replayId:'r',confidence:1,capturedAt:'x'}],domains:['KUBERNETES']}]); assert.equal(out.category,'AUTOSCALING_INEFFICIENCY');});
