import test from "node:test"; import assert from "node:assert/strict"; import * as m from "../lib/execution-replay-governance/execution-learning-signal"; test("execution-learning-signal",()=>{ assert.equal(Object.values(m).length>0,true); });