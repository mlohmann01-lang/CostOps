import test from'node:test';import assert from'node:assert/strict';import{ProcurementApProductionService,createProcurementApFetchFixture,procurementApProfiles}from'../lib/production-connectors/procurement-ap';
test('Procurement/AP discovery and profiles work',async()=>{assert.equal(procurementApProfiles.length,6);const svc=new ProcurementApProductionService({fetchImpl:createProcurementApFetchFixture() as any});await svc.connect({tenantId:'pap',profileKey:'COUPA',baseUrl:'https://proc.example.com',credentials:{authType:'API_TOKEN',apiToken:'token'},mode:'LIVE'});const d=await svc.discover('pap');assert.ok(d.vendors.length>0);assert.ok(d.contracts.length>0);});
