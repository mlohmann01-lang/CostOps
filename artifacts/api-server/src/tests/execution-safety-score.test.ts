import test from "node:test"; import assert from "node:assert/strict"; import * as m from "../lib/execution-simulation/execution-safety-score"; test("execution-safety-score",()=>{ assert.equal(Object.values(m).length>0,true); });