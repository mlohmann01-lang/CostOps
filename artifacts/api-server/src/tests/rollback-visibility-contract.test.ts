import test from "node:test"; import assert from "node:assert/strict"; import * as m from "../lib/execution-ux-contracts/rollback-visibility-contract"; test("rollback-visibility-contract",()=>{ assert.equal(Object.values(m).length>0,true); });
