import test from 'node:test'; import assert from 'node:assert/strict'; import { calculateEntityReliability, recordCalibrationMemory } from '../lib/economic-memory';
test('entity reliability returns grouped entities',()=>{ const rs=[recordCalibrationMemory({tenantId:'t',sourceType:'ENTITY',sourceId:'1',domain:'AI_ECONOMICS',entityId:'team-1',entityType:'team',playbookId:'p',forecastId:'f',scenarioId:'s',recommendationId:'r',calibrationType:'PRODUCTIVITY',expectedValue:100,realizedValue:80,confidenceBefore:0.7,confidenceAfter:0.55,evidenceRefs:['z'],replayCorrelationId:'x',lineageCorrelationId:'y'})]; const out=calculateEntityReliability(rs); assert.equal(out[0].entityId,'team-1'); });