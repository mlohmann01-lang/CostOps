import test from'node:test';import assert from'node:assert/strict';import{ErpProductionService,createErpFetchFixture}from'../lib/production-connectors/erp';
test('ERP normalises financial truth records',async()=>{const svc=new ErpProductionService({fetchImpl:createErpFetchFixture() as any});await svc.connect({tenantId:'erpf',profileKey:'SAP',baseUrl:'https://erp.example.com',credentials:{authType:'API_TOKEN',apiToken:'token'},mode:'LIVE'});await svc.discover('erpf');assert.ok((await (svc as any).deps.financial.listInvoices('erpf')).length>=1);assert.ok((await (svc as any).deps.financial.listPurchaseOrders('erpf')).length>=1);assert.ok((await (svc as any).deps.financial.listCostCentres('erpf')).length>=1);assert.ok((await (svc as any).deps.financial.listBudgets('erpf')).length>=1);assert.ok((await (svc as any).deps.financial.listVendorSpend('erpf')).length>=1);});
