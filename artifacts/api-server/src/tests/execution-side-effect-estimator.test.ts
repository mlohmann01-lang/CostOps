import test from "node:test"; import assert from "node:assert/strict"; import * as m from "../lib/execution-simulation/execution-side-effect-estimator"; test("execution-side-effect-estimator",()=>{ assert.equal(Object.values(m).length>0,true); });