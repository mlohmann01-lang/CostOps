import test from "node:test"; import assert from "node:assert/strict"; import { arbitrateRecommendationCandidates, detectRecommendationConflicts, evaluateOperationalSensitivity, calculateRecommendationConfidence } from "../lib/decision-intelligence"; import { toM365RecommendationCandidate } from "../lib/decision-intelligence/m365-expansion-pack-1.js";
const pb=(id:string,action:string)=>({id,name:id,action,defaultExecutionMode:"APPROVAL_REQUIRED",riskClass:"B"} as any);
test("arbitration integration returns deterministic final states",()=>{ const a=toM365RecommendationCandidate("t",pb("a","RECLAIM"),{email:"u@c.com",displayName:"U",sku:"E3",cost:1,days:100}); const b=toM365RecommendationCandidate("t",pb("b","RECLAIM"),{email:"u@c.com",displayName:"U",sku:"E3",cost:1,days:100}); const conflicts=detectRecommendationConflicts([a,b]); const s=[a,b].map(evaluateOperationalSensitivity); const conf=[a,b].map((c,i)=>calculateRecommendationConfidence(c,s[i])); const out=arbitrateRecommendationCandidates([a,b],{conflictResults:conflicts,sensitivityResults:s,confidenceResults:conf}); assert.equal(out.some(x=>x.finalDecision==="SUPPRESSED_DUPLICATE"), true);});
