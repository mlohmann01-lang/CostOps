import test from "node:test"; import assert from "node:assert/strict"; import * as m from "../lib/canonical-runtime-orchestration/canonical-runtime-transition-guard"; test("canonical-runtime-transition-guard",()=>{ assert.equal(Object.values(m).length>0,true); });