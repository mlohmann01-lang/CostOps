import test from'node:test';import assert from'node:assert/strict';import{ServiceNowProductionService,createServiceNowFetchFixture}from'../lib/production-connectors/servicenow';
test('ServiceNow maps users groups and owner assignments',async()=>{const svc=new ServiceNowProductionService({fetchImpl:createServiceNowFetchFixture() as any});await svc.connect({tenantId:'snowo',instanceUrl:'https://tenant.service-now.com',credentials:{authType:'API_TOKEN',apiToken:'token'},mode:'LIVE'});await svc.discover('snowo');assert.equal((await (svc as any).deps.ownership.listUsers('snowo')).length,1);assert.equal((await (svc as any).deps.ownership.listDepartments('snowo')).length,1);assert.equal((await (svc as any).deps.ownership.listAssignments('snowo')).length,1);});
