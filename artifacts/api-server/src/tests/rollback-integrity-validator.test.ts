import test from "node:test"; import assert from "node:assert/strict"; import * as x from "../lib/post-execution-verification/rollback-integrity-validator"; test("rollback-integrity-validator",()=>{ assert.equal(Object.values(x).length>0,true); });
