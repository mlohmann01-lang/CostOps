import test from'node:test';import assert from'node:assert/strict';import{ProcurementApProductionService,createProcurementApFetchFixture}from'../lib/production-connectors/procurement-ap';
test('Procurement/AP normalises financial records',async()=>{const svc=new ProcurementApProductionService({fetchImpl:createProcurementApFetchFixture() as any});await svc.connect({tenantId:'papf',profileKey:'COUPA',baseUrl:'https://proc.example.com',credentials:{authType:'API_TOKEN',apiToken:'token'},mode:'LIVE'});await svc.discover('papf');assert.ok((await (svc as any).deps.financial.listPurchaseOrders('papf')).length>=1);assert.ok((await (svc as any).deps.financial.listInvoices('papf')).length>=1);assert.ok((await (svc as any).deps.financial.listCostCentres('papf')).length>=1);});
