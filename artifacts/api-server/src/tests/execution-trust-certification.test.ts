import test from "node:test"; import assert from "node:assert/strict"; import * as m from "../lib/execution-certification/execution-trust-certification"; test("execution-trust-certification",()=>{ assert.equal(Object.values(m).length>0,true); });