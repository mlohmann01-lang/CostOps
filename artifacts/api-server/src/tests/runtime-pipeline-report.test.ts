import test from "node:test"; import assert from "node:assert/strict"; import * as m from "../lib/canonical-runtime-pipeline/runtime-pipeline-report"; test("runtime-pipeline-report",()=>{ assert.equal(Object.values(m).length>0,true); });