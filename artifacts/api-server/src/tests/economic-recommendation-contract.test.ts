import test from 'node:test'; import assert from 'node:assert/strict'; import { buildEconomicRecommendationContract } from '../lib/economic-intelligence-kernel'; test('requires evidence and no mutation payload',()=>{assert.throws(()=>buildEconomicRecommendationContract({domain:'CLOUD',title:'mutate now',reviewMode:'RECOMMEND_ONLY',confidence:0.9,severity:0.9,evidenceReferences:[{id:'1',source:'s',lineageId:'l',replayId:'r',confidence:1,capturedAt:'x'}]}));});
