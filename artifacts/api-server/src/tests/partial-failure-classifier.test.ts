import test from 'node:test';import assert from 'node:assert/strict';import { classifyFailure } from '../lib/partial-failure-semantics/partial-failure-classifier';
test('provider-specific failures classified',()=>{ assert.equal(classifyFailure({preflight:true,provider:'ENTRA_SYNC_PENDING',postflight:true,verified:true}),'ENTRA_SYNC_PENDING'); assert.equal(classifyFailure({preflight:true,provider:'ORACLE_ENTITLEMENT_AMBIGUOUS',postflight:true,verified:true}),'ORACLE_ENTITLEMENT_AMBIGUOUS'); assert.equal(classifyFailure({preflight:true,provider:'CLOUD_TAG_PROPAGATION_DELAY',postflight:true,verified:true}),'CLOUD_TAG_PROPAGATION_DELAY');});
