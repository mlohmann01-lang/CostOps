import test from 'node:test'; import assert from 'node:assert/strict'; import { evaluateGovernanceDrift } from '../lib/economic-memory-drift'; test('approval delay',()=>{assert.ok(evaluateGovernanceDrift([{...{tenantId:'t',domain:'CLOUD',category:'x',timestamp:'2026-01-01',savingsDelta:-1,evidenceReferences:[{id:'1',source:'s',lineageId:'l',replayId:'r',confidence:1,capturedAt:'t'}],approved:true},approved:false}]).approvalDelay>0);});
