import test from 'node:test'; import assert from 'node:assert/strict'; import { normalizeEconomicCausalSignals } from '../lib/economic-causality'; test('normalizes and sorts',()=>{const out=normalizeEconomicCausalSignals([{category:'USAGE_DECLINE',strength:2,evidenceReferences:[],domains:['CLOUD']} as any,{category:'GPU_FRAGMENTATION',strength:0.4,evidenceReferences:[],domains:['AI_RUNTIME']} as any]); assert.equal(out[0].strength,1);});
