import test from "node:test"; import assert from "node:assert/strict"; import { evaluateCoefficientSensitivity } from "../lib/quantitative-depth";
test("higher value gives higher impact",()=>{ const r=evaluateCoefficientSensitivity({coefficients:[{id:'a',coefficientClass:'QUERY_INTENSITY',value:0.2,range:{min:0,max:1},source:'s',confidence:1,version:'1.0.0',explanation:'',assumptions:[],evidenceRefs:['e']},{id:'b',coefficientClass:'QUERY_INTENSITY',value:0.9,range:{min:0,max:1},source:'s',confidence:1,version:'1.0.0',explanation:'',assumptions:[],evidenceRefs:['e']}]}); assert.equal(r[1].impactScore>r[0].impactScore,true);});
