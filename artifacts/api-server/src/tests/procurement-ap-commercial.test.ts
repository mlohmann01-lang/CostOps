import test from'node:test';import assert from'node:assert/strict';import{ProcurementApProductionService,createProcurementApFetchFixture}from'../lib/production-connectors/procurement-ap';
test('Procurement/AP normalises commercial records',async()=>{const svc=new ProcurementApProductionService({fetchImpl:createProcurementApFetchFixture() as any});await svc.connect({tenantId:'papc',profileKey:'COUPA',baseUrl:'https://proc.example.com',credentials:{authType:'API_TOKEN',apiToken:'token'},mode:'LIVE'});await svc.discover('papc');assert.equal((await (svc as any).deps.commercial.listVendors('papc')).length,2);assert.equal((await (svc as any).deps.commercial.listContracts('papc')).length,1);assert.equal((await (svc as any).deps.commercial.listRenewals('papc')).length,1);});
