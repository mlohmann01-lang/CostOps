import test from 'node:test';import assert from 'node:assert/strict';import { detectCircularDependencies } from '../lib/execution-dependency-graph/execution-dependency-graph';
test('circular dependencies detected',()=>{ assert.equal(detectCircularDependencies([{id:'A',risk:'LOW'},{id:'B',risk:'LOW'}],[{from:'A',to:'B',type:'ASSET_DEPENDENCY'},{from:'B',to:'A',type:'ASSET_DEPENDENCY'}]),true); assert.equal(detectCircularDependencies([{id:'A',risk:'LOW'},{id:'B',risk:'LOW'}],[{from:'A',to:'B',type:'ASSET_DEPENDENCY'}]),false); assert.equal(typeof detectCircularDependencies, 'function');});
