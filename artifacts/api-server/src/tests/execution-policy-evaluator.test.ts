import test from "node:test"; import assert from "node:assert/strict"; import * as m from "../lib/execution-policy-engine/execution-policy-evaluator"; test("execution-policy-evaluator",()=>{ assert.equal(Object.values(m).length>0,true); });