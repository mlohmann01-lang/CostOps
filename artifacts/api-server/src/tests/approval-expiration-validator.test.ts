import test from "node:test"; import assert from "node:assert/strict"; import * as x from "../lib/execution-window-enforcement/approval-expiration-validator"; test("approval-expiration-validator",()=>{ assert.equal(Object.values(x).length>0,true); });