import test from "node:test"; import assert from "node:assert/strict"; import * as x from "../lib/governed-live-connectors/governed-live-cloud"; test("governed-live-cloud",()=>{ assert.equal(Object.values(x).length>0,true); });