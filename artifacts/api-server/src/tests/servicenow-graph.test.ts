import test from'node:test';import assert from'node:assert/strict';import{ServiceNowProductionService,createServiceNowFetchFixture}from'../lib/production-connectors/servicenow';
test('ServiceNow graph nodes and edges generated',async()=>{const graph={nodes:[] as any[],edges:[] as any[],createNode:async(x:any)=>graph.nodes.push(x),createEdge:async(x:any)=>graph.edges.push(x)};const svc=new ServiceNowProductionService({fetchImpl:createServiceNowFetchFixture() as any,graph});await svc.connect({tenantId:'snowg',instanceUrl:'https://tenant.service-now.com',credentials:{authType:'API_TOKEN',apiToken:'token'},mode:'LIVE'});await svc.discover('snowg');assert.ok(graph.nodes.length>0);assert.ok(graph.edges.length>0);});
