import test from "node:test"; import assert from "node:assert/strict"; import * as m from "../lib/execution-policy-engine/rollback-mandate-policy"; test("rollback-mandate-policy",()=>{ assert.equal(Object.values(m).length>0,true); });