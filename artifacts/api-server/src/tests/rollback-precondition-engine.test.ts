import test from "node:test"; import assert from "node:assert/strict"; import * as x from "../lib/rollback-enforcement/rollback-precondition-engine"; test("rollback-precondition-engine",()=>{ assert.equal(Object.values(x).length>0,true); });