import test from 'node:test'; import assert from 'node:assert/strict'; import { computeProductionRuntimeDiagnostics } from '../lib/runtime-hardening/runtime-hardening-phase-a';
test('runtime diagnostics readiness aggregate',()=>{const r=computeProductionRuntimeDiagnostics({persistenceIntegrityHealth:'HEALTHY',replayDurabilityHealth:'HEALTHY',telemetryRetentionHealth:'HEALTHY',workflowAccumulationHealth:'HEALTHY',reconciliationPersistenceHealth:'HEALTHY',graphScaleHealth:'HEALTHY',tenantIsolationHealth:'HEALTHY',outcomeCalibrationHealth:'HEALTHY',simulationReliabilityHealth:'HEALTHY'});assert.equal(r.productionRuntimeReadiness,'READY');});
