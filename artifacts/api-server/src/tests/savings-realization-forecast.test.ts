import test from 'node:test'; import assert from 'node:assert/strict'; import { forecastSavingsRealization } from '../lib/economic-forecasting';
test('low confidence and duplicate risk reduce realization',()=>{ const a=forecastSavingsRealization({tenantId:'t',forecastWindow:'90D',recommendationId:'r',projectedSavings:100,recommendationConfidence:0.9,historicalReversals:0.1,overrideRate:0.1,ownershipClarity:0.9,operationalSensitivity:0.2,duplicateAttributionRisk:0.1,governanceRisk:0.2}); const b=forecastSavingsRealization({tenantId:'t',forecastWindow:'90D',recommendationId:'r',projectedSavings:100,recommendationConfidence:0.3,historicalReversals:0.4,overrideRate:0.5,ownershipClarity:0.4,operationalSensitivity:0.2,duplicateAttributionRisk:0.7,governanceRisk:0.4}); assert.equal((a.projectedSavingsRealizationRate ?? 0)>(b.projectedSavingsRealizationRate ?? 0),true);});
