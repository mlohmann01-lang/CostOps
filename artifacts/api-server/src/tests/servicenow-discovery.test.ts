import test from'node:test';import assert from'node:assert/strict';import{ServiceNowProductionService,createServiceNowFetchFixture}from'../lib/production-connectors/servicenow';
test('ServiceNow discovery and capabilities work',async()=>{const svc=new ServiceNowProductionService({fetchImpl:createServiceNowFetchFixture() as any});await svc.connect({tenantId:'snow',instanceUrl:'https://tenant.service-now.com',credentials:{authType:'API_TOKEN',apiToken:'token'},mode:'LIVE'});const d=await svc.discover('snow');assert.equal(d.businessApplications.length,1);assert.ok(d.capabilities.some(c=>c.table==='cmdb_ci_business_app'&&c.available));});
