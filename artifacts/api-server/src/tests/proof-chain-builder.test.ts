import test from "node:test"; import assert from "node:assert/strict"; import * as m from "../lib/runtime-proof-continuity/proof-chain-builder"; test("proof-chain-builder",()=>{ assert.equal(Object.values(m).length>0,true); });