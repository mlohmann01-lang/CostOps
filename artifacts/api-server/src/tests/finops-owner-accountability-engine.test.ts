import test from 'node:test'; import assert from 'node:assert/strict'; import { deriveFinOpsOwnerAccountability, normalizeFinOpsSourceRecommendation } from '../lib/finops-operationalization';
test('derives owner accountability',()=>{ const n=normalizeFinOpsSourceRecommendation({tenantId:'t',sourceSystem:'AWS',sourceRecommendationId:'1',cloudProvider:'AWS',recommendationCategory:'OWNER_REMEDIATION',recommendationSummary:'owner',estimatedMonthlySavings:20,savingsConfidence:0.5,ownerHints:['team-a'],sourceCreatedAt:'a',sourceUpdatedAt:'b',lineageCorrelationId:'l',replayCorrelationId:'r'}); const o=deriveFinOpsOwnerAccountability(n,{assignmentGroupHint:'grp'}); assert.equal(o.ownerId,'team-a'); });