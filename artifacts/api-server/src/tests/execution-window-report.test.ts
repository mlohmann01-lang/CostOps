import test from "node:test"; import assert from "node:assert/strict"; import * as x from "../lib/execution-window-enforcement/execution-window-report"; test("execution-window-report",()=>{ assert.equal(Object.values(x).length>0,true); });