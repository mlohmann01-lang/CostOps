import test from "node:test"; import assert from "node:assert/strict"; import fs from "node:fs";
test("replay completeness statuses supported", ()=>{ const s=fs.readFileSync(new URL("../lib/playbooks/playbook-recommendation-service.ts", import.meta.url),"utf8"); for (const k of ["VALID","PARTIAL","INCOMPLETE","MISMATCH","missingTransitions","hashMismatch","workflowMismatch","outcomeMismatch","simulationMismatch"]) assert.equal(s.includes(k), true); const telem=fs.readFileSync(new URL("../lib/observability/operational-telemetry-service.ts", import.meta.url),"utf8"); assert.equal(telem.includes("ATLASSIAN_REPLAY_MISMATCH"), true); assert.equal(telem.includes("ATLASSIAN_EXECUTIVE_REPORT_GENERATED"), true); assert.equal(telem.includes("CROSS_DOMAIN_REPLAY_MISMATCH"), true); assert.equal(telem.includes("RUNTIME_HARDENING_REPLAY_DURABILITY_EVALUATED"), true); });
