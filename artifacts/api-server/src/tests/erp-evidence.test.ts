import test from'node:test';import assert from'node:assert/strict';import{ErpProductionService,createErpFetchFixture}from'../lib/production-connectors/erp';
test('ERP evidence generated',async()=>{const svc=new ErpProductionService({fetchImpl:createErpFetchFixture() as any});await svc.connect({tenantId:'erpe',profileKey:'SAP',baseUrl:'https://erp.example.com',credentials:{authType:'API_TOKEN',apiToken:'token'},mode:'LIVE'});await svc.discover('erpe');assert.ok((await (svc as any).deps.evidence.listEvidenceRecords('erpe')).length>0);assert.ok((await (svc as any).deps.evidence.listArtifacts('erpe')).length>0);});
