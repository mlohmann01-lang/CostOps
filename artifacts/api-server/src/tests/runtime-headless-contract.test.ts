import test from "node:test"; import assert from "node:assert/strict"; import * as m from "../lib/runtime-interface-contracts/runtime-headless-contract"; test("runtime-headless-contract",()=>{ assert.equal(Object.values(m).length>0,true); });