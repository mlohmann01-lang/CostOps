import { Router } from 'express'; import { liveTenantReadinessService as svc, liveTenantReadinessRepository as repo, runLiveTenantReadinessAudit } from '../lib/live-tenant-readiness';
const r=Router(); const tenant=(req:any)=>String(req.header('x-tenant-id')??req.query.tenantId??req.body?.tenantId??'default-tenant'); const send=(fn:any)=>async(req:any,res:any)=>{try{res.json(await fn(req,res))}catch(e:any){res.status(400).json({error:e.message??String(e)})}};
r.post('/profiles',send((req:any)=>svc.createTenantProfile({...req.body,tenantId:tenant(req)}))); r.get('/profiles',send((req:any)=>repo.listTenantProfiles(tenant(req),req.query))); r.get('/profiles/latest',send((req:any)=>repo.getLatestTenantProfile(tenant(req))));
r.post('/connectors',send((req:any)=>svc.upsertConnectorReadiness({...req.body,tenantId:tenant(req)}))); r.get('/connectors',send((req:any)=>repo.listConnectorReadiness(tenant(req),req.query)));
r.post('/data-coverage',send((req:any)=>svc.upsertDataCoverage({...req.body,tenantId:tenant(req)}))); r.get('/data-coverage',send((req:any)=>repo.listDataCoverage(tenant(req),req.query)));
r.post('/checks',send((req:any)=>svc.upsertReadinessCheck({...req.body,tenantId:tenant(req)}))); r.get('/checks',send((req:any)=>repo.listReadinessChecks(tenant(req),req.query)));
r.post('/onboarding-steps',send((req:any)=>svc.upsertOnboardingStep({...req.body,tenantId:tenant(req)}))); r.get('/onboarding-steps',send((req:any)=>repo.listOnboardingSteps(tenant(req),req.query)));
r.post('/safety-gates',send((req:any)=>svc.upsertSafetyGate({...req.body,tenantId:tenant(req)}))); r.get('/safety-gates',send((req:any)=>repo.listSafetyGates(tenant(req),req.query)));
r.post('/blockers',send((req:any)=>svc.upsertBlocker({...req.body,tenantId:tenant(req)}))); r.get('/blockers',send((req:any)=>repo.listBlockers(tenant(req),req.query)));
r.post('/snapshots/build',send((req:any)=>svc.buildReadinessSnapshot(tenant(req)))); r.get('/snapshots',send((req:any)=>repo.listReadinessSnapshots(tenant(req),req.query))); r.get('/summary',send((req:any)=>svc.summariseLiveTenantReadiness(tenant(req)))); r.post('/onboarding-plan/generate',send((req:any)=>svc.generateOnboardingPlan(tenant(req)))); r.post('/blockers/generate',send((req:any)=>svc.generateReadinessBlockers(tenant(req)))); r.post('/approve-live-mode',send((req:any)=>svc.approveLiveMode(tenant(req),String(req.body?.approvedByUserId??req.header('x-user-id')??'system')))); r.get('/audit',send(()=>runLiveTenantReadinessAudit()));
export default r;
