import { Shell } from '../components/layout/Shell';import { EmptyState, ExecutivePageHeader, ExecutiveSection, MetricCard, StatusChip } from '../components/executive';import { notAvailable } from '../types/evidenceRegistry';import { useEvidenceRegistry } from '../hooks/useEvidenceRegistry';import { DataStateBanner } from '../components/shared/DataStateBanner';
const val=(v:any)=>v===undefined||v===null?notAvailable:v;const pct=(v:any)=>v===undefined||v===null?notAvailable:`${v}%`;const tone=(s:string)=>/FAIL|BLOCK|MISSING|SECRET|RESTRICTED|PENDING/i.test(s)?'danger':/PARTIAL|WARN|MEDIUM/i.test(s)?'warning':/READY|VALIDATED|PASS|DEMO|HIGH/i.test(s)?'success':'info' as any;
export function renderEvidenceRegistryState(state:any){return {title:'Evidence Registry',demoBanner:state.isDemo?'Demo evidence metrics':'',liveEmpty:!state.isDemo&&state.unavailable,records:state.records?.length??0,unknown:val(undefined),readiness:state.snapshot?.readiness??notAvailable}}
export default function EvidenceRegistry(){const state=useEvidenceRegistry();const s=state.snapshot;const rows=state.records??[];const selected=state.selectedChain??(rows[0]?{record:rows[0],artifacts:[],links:[],provenance:[],integrityChecks:[],lifecycleEvents:[],exports:[]}:null);return <Shell><main style={{padding:'24px clamp(18px,3vw,34px)',display:'grid',gap:16,maxWidth:1480,margin:'0 auto'}}><ExecutivePageHeader title="Evidence Registry" subtitle="Governed evidence metadata, provenance, integrity, retention, redaction, and linkage across Certen." chips={[{label:`Tenant mode: ${state.isDemo?'Demo':'Live'}`,tone:state.isDemo?'info':'warning'},{label:`Evidence readiness: ${s?.readiness??notAvailable}`,tone:tone(s?.readiness??'MISSING')},{label:`Last updated: ${s?.generatedAt?new Date(s.generatedAt).toLocaleString():notAvailable}`,tone:'info'}]}/>{state.dataState!=='LIVE'&&<DataStateBanner state={state.dataState} detail={state.error} ctaLabel={state.dataState==='NOT_CONNECTED'?'Connect Tenant':undefined} ctaHref={state.dataState==='NOT_CONNECTED'?'/connectors':undefined}/>}{!state.isDemo&&state.unavailable&&<EmptyState title="Evidence registry unavailable." description="Evidence will appear once connectors, authorities, or portfolio records produce evidence refs."/>}<section style={{display:'grid',gridTemplateColumns:'repeat(4,minmax(170px,1fr))',gap:12}}>{[['Evidence records',s?.evidenceCount],['Validated',s?.validatedCount],['Missing',s?.missingCount],['Failed',s?.failedCount],['Integrity score',pct(s?.integrityScore)],['Provenance score',pct(s?.provenanceScore)],['Redaction pending',s?.redactionPendingCount],['Restricted evidence',s?.restrictedCount]].map(([label,value])=><MetricCard key={label as string} label={label as string} value={val(value)} description="Verified registry metric"/> )}</section><ExecutiveSection title="Records" description="Evidence · Type · Source · Target · Status · Trust · Classification · Integrity · Redaction · Expires"><div style={{overflowX:'auto'}}><table style={{width:'100%',borderCollapse:'collapse'}}><thead><tr>{['Evidence','Type','Source','Target','Status','Trust','Classification','Integrity','Redaction','Expires'].map(h=><th key={h} style={{textAlign:'left',padding:8}}>{h}</th>)}</tr></thead><tbody>{rows.map((r:any)=><tr key={r.id}><td>{r.title}</td><td>{r.evidenceType}</td><td>{r.sourceSystem}</td><td>{r.targetType}:{r.targetId}</td><td><StatusChip label={r.status} tone={tone(r.status)}/></td><td>{r.trustLevel}</td><td>{r.classification}</td><td>{r.metadata?.integrity??notAvailable}</td><td>{r.redactionStatus}</td><td>{r.expiresAt?new Date(r.expiresAt).toLocaleDateString():notAvailable}</td></tr>)}</tbody></table></div></ExecutiveSection><section style={{display:'grid',gridTemplateColumns:'repeat(3,1fr)',gap:12}}>{['Gaps','Integrity','Provenance','Retention','Redaction','Exports'].map(name=><ExecutiveSection key={name} title={name} description={`${name} section`}><p>{name==='Gaps'?`${s?.criticalGaps?.length??notAvailable} critical gaps`:name+' details render when evidence is available.'}</p></ExecutiveSection>)}</section><ExecutiveSection title="Evidence chain" description="Provenance timeline, linked targets, artifacts, integrity checks, lifecycle events, and exports.">{selected?<div data-testid="evidence-chain-panel" style={{display:'grid',gridTemplateColumns:'repeat(3,1fr)',gap:12}}><div><strong>Provenance timeline</strong>{selected.provenance.map((p:any,i:number)=><p key={i}>{p.eventType} · {p.occurredAt}</p>)}</div><div><strong>Linked targets</strong>{selected.links.map((l:any,i:number)=><p key={i}>{l.targetType}:{l.targetId}</p>)}</div><div><strong>Artifacts</strong>{selected.artifacts.map((a:any)=><p key={a.id}>{a.id}</p>)}</div><div><strong>Integrity checks</strong>{selected.integrityChecks.map((c:any,i:number)=><p key={i}>{c.checkType} {c.status}</p>)}</div><div><strong>Lifecycle events</strong>{selected.lifecycleEvents.map((l:any,i:number)=><p key={i}>{l.toStatus}</p>)}</div><div><strong>Exports</strong>{selected.exports.length||notAvailable}</div></div>:<p>{notAvailable}</p>}</ExecutiveSection></main></Shell>}
