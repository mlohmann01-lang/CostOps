import { Layout } from "@/components/layout";
export default function SupportDiagnosticsPage(){ return <Layout><div><h1 className="text-2xl font-semibold">Support Diagnostics</h1><p>Tenant-scoped support diagnostics for connector health, sync status, telemetry, policies, workflow, and SLA exceptions.</p></div></Layout>; }
