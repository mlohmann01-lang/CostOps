{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test": "pnpm --filter @workspace/api-server run test:contoso && pnpm --filter @workspace/api-server run test:platform-boundaries",
    "seed:orchestration-demo": "pnpm --filter @workspace/db build && pnpm --filter @workspace/scripts exec tsx ./seed-execution-orchestration-demo.ts",
    "seed:customer-demo:m365": "pnpm --filter @workspace/db build && pnpm --filter @workspace/scripts exec tsx ./seed-customer-demo-m365.ts",
    "validate:runtime-env": "pnpm --filter @workspace/scripts exec tsx scripts/validate-runtime-env.ts",
    "smoke:m365-demo": "pnpm --filter @workspace/scripts exec tsx scripts/smoke-m365-demo.ts"
  },
  "packageManager": "pnpm@10.33.0",
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
